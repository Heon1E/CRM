# 대표님이 직접 하셔야 하는 일

제가 코드로 못 하는 것들입니다. **위에서부터 급한 순서**입니다.
하나씩 마치고 지우시면 됩니다.

---

## 1. ✅ Supabase 가입 막기 — 끝났습니다

확인했습니다(계정을 만들지 않고 인증 설정만 읽었습니다):

```
disable_signup : true    ← 새로 가입할 수 없습니다
```

이제 두 겹이 다 막혔습니다 — 바깥은 가입 자체가 안 되고, 혹시 뚫려도
새 계정은 `pending`이라 아무것도 못 봅니다.

참고로 **구글 로그인은 켜져 있습니다.** 가입이 막혀 있으므로 새 구글 계정은
들어오지 못하고, 대표님 `heoniree@gmail.com`은 이미 있는 계정이라 그대로
쓰실 수 있습니다. 그대로 두시면 됩니다.

---

## 2. 🔴 Vercel 환경변수 2개

### 어디로 가는가

> **https://vercel.com/heonirees-projects/crm-dashboard/settings/environment-variables**
>
> (또는 vercel.com → `crm-dashboard` 프로젝트 → 위쪽 **Settings** 탭
> → 왼쪽 메뉴 **Environment Variables**)

### 넣을 것 ① — 텔레그램 봇·아침 브리핑용

| 칸 | 넣을 값 |
|---|---|
| **Key** | `SUPABASE_SERVICE_ROLE_KEY` |
| **Value** | 아래에서 복사 |
| **Environments** | **Production · Preview · Development 셋 다 체크** |

값 가져오는 곳:
> **https://supabase.com/dashboard/project/dfukyqrradgmytyqxvnw/settings/api**
> → **Project API keys** → **`service_role`** 줄의 **Reveal** 을 누르고 복사
> (`anon` 말고 **service_role** 입니다. `eyJ...` 로 시작하는 긴 문자열)

> ⚠️ **이름 앞에 `VITE_` 를 붙이면 안 됩니다.**
> 붙이면 브라우저에 그대로 박혀 공개됩니다. 이 키는 RLS를 통째로 우회하므로
> 노출되면 누구나 전 데이터를 읽고 지울 수 있습니다.

### 넣을 것 ② — 배포된 사이트의 지도용

| 칸 | 넣을 값 |
|---|---|
| **Key** | `VITE_KAKAO_MAP_KEY` |
| **Value** | `.env`에 넣으신 값 그대로 (`7393…` 로 시작하는 32자) |
| **Environments** | **셋 다 체크** |

이건 반대로 **`VITE_` 가 반드시 붙어야** 합니다 — 브라우저가 읽는 값입니다.
카카오 JS 키는 공개돼도 되는 키이고, 도메인 등록으로 보호됩니다.

### 마지막 — 재배포

**환경변수는 넣기만 하면 반영되지 않습니다.** 이미 배포된 것은 예전 값으로 굳어 있습니다.

> Vercel 위쪽 **Deployments** 탭 → 맨 위 배포의 오른쪽 **⋯** → **Redeploy**
> → (Use existing Build Cache 체크 해제 권장) → **Redeploy**

### 잘 됐는지 확인하는 법

재배포가 끝나면 알려 주세요. 제가 확인하겠습니다. 직접 보시려면:

- 지도 — 배포 주소에서 `/map` 을 열었을 때 "카카오 지도 키가 없습니다" 안내가
  **사라지고** 지도가 뜨면 성공입니다.
- 텔레그램 — 봇에게 아무 말이나 보냈을 때 답이 오면 성공입니다.
  (`SUPABASE_SERVICE_ROLE_KEY` 가 없으면 봇이 조용하거나 오류를 냅니다.)

---

## 3. 🟡 SQL 한 개 실행 — 개인 설정이 저장되지 않습니다

설정 화면을 열면 콘솔에
`new row violates row-level security policy for table "settings"` 가 뜹니다.
알림 켜고 끄기가 저장되지 않습니다(업무는 막지 않습니다).

> Supabase → **SQL Editor** → 아래 파일 내용을 붙여넣고 실행
>
> ```
> execution/sql/settings_rls.sql
> ```

---

## 4. ✅ 카카오 지도 — 끝났습니다

키를 넣으시자 지도가 살아났습니다. 실측: 타일 36장, 마커 27개(거래처 26 + 본사),
마커를 누르면 거래처 카드가 뜹니다. **구글 클라우드 결제는 하지 않으셔도 됩니다.**

남은 것 하나 — **앱 대표 도메인이 틀렸습니다.**

지금 값 `https://crm-dashboard-g7zrdqqbp-heonirees-projects` 는 두 가지가 문제입니다:
1. `.vercel.app` 이 빠져 있어 **존재하지 않는 주소**입니다
2. 가운데 `g7zrdqqbp` 는 **배포할 때마다 새로 생기는 번호**입니다 — 다음 배포에 어긋납니다

> 앱 설정 → 일반 → 앱 대표 도메인 → 아래 값으로 수정
> ```
> https://crm-dashboard-blush.vercel.app
> ```
> (제가 접속해서 아이앤디 CRM이 뜨는 것을 확인했습니다)

**JS SDK 도메인 목록도 같이 확인해 주세요.** 거기에도 배포별 주소가 들어가 있으면
다음 배포 때 지도가 멈춥니다. 아래 둘만 있으면 됩니다:
```
http://localhost:5173
https://crm-dashboard-blush.vercel.app
```

Vercel 환경변수에 `VITE_KAKAO_MAP_KEY` 를 넣는 것도 잊지 마세요(2번 항목과 같은 화면).

## 5. 🔴 Vercel에 `CRON_SECRET` 넣기 — 지금 아침 브리핑 주소가 열려 있습니다

배포된 주소에 대고 직접 확인했습니다:

```
curl -H "x-vercel-cron: 1" https://crm-dashboard-blush.vercel.app/api/daily-digest
→ HTTP 200 (브리핑이 실제로 발송됩니다)
```

이 주소는 공개돼 있습니다. **지금은 누구나 이 한 줄로 대표님 텔레그램에
브리핑을 마음대로 쏘아 보낼 수 있습니다.** (자료가 새는 것은 아닙니다 —
메시지는 등록된 대화로만 갑니다. 다만 스팸이 가능합니다.)

막는 방법은 Vercel 공식 방식인 `CRON_SECRET` 입니다. 이 값이 있으면
Vercel이 크론 호출에 `Authorization: Bearer <값>` 을 붙여 주고, 코드는
그것만 통과시킵니다. 바깥에서는 값을 모르니 흉내 낼 수 없습니다.

> Vercel → Settings → Environment Variables → 추가
>
> | 칸 | 값 |
> |---|---|
> | **Key** | `CRON_SECRET` |
> | **Value** | 아무 긴 문자열 (예: 비밀번호 생성기로 만든 32자 이상) |
> | **Environments** | 셋 다 체크 |
>
> 넣으신 뒤 **Redeploy** 해야 반영됩니다.

값은 아무도 외울 필요 없습니다. Vercel과 코드만 알면 됩니다.

---

## 6. 🟡 Vercel 크론이 실제로 도는지 확인 (제가 못 보는 화면)

브리핑 함수는 **부르면 정상 동작합니다** — 제가 직접 호출해 확인했습니다.
그런데 8/12부터 나흘 동안 아침에 오지 않았습니다. 즉 **Vercel이 함수를
부르지 않고 있을 가능성**이 큽니다.

크롬 확장이 `vercel.com` 접속을 막아 제가 볼 수 없습니다. 봐 주세요:

> Vercel → 프로젝트 → **Settings** → **Cron Jobs**

- 목록에 `/api/daily-digest` 가 **보이는지**
- **Last Run** 기록이 있는지, 있다면 성공인지 실패인지

| 무엇이 보이나 | 뜻 |
|---|---|
| 목록에 아예 없다 | 크론이 등록되지 않았습니다 — 알려 주시면 다른 방법을 찾겠습니다 |
| 있는데 Last Run 없음 | 한 번도 안 불렸습니다 |
| 있고 실패로 찍힘 | 방금 고친 것이 맞습니다 |

---

## 7. 🟡 개발 편의 — 자동 로그인 (선택)

넣어 두시면 제가 개발 중에 매번 로그인하지 않아도 됩니다.
`.env.local` 파일 맨 아래에 두 줄:

```
VITE_DEV_AUTOLOGIN_ID=heoniree
VITE_DEV_AUTOLOGIN_PW=<비밀번호>
```

`.env.local`은 `.gitignore` 대상이고, 이 기능은 `import.meta.env.DEV` 안에 있어
**배포 빌드에서는 통째로 잘려 나갑니다.**

---

## 6. 🟢 확인만 해 주시면 되는 것들

### (a) 파이프라인 기준이 실제와 맞습니까

- 단계: `리드 · 접촉 · 제안 · 샘플 · 협상` → `수주 / 실패`
- 단계별 확률: 10% · 25% · 45% · 60% · 80%
- '멈춘 건' 판정: **샘플 60일**, 나머지 21~30일

샘플 테스트가 실제로 몇 달 걸린다고 하셨는데, 60일이 맞습니까?

### (b) 합칠지 판단이 필요한 거래처

전화번호가 같은데 거래처가 다릅니다. 회사를 옮긴 것인지, 거래처가 중복
등록된 것인지 제가 알 수 없습니다.

| 번호 | 이름 | 한쪽 | 다른쪽 |
|---|---|---|---|
| 010-5006-3496 | 허문희 | 성신드럼 | (주)현대그린텍 |
| 010-5064-4806 | 청양 심관섭 | 애경화학(주) | 애경케미칼(주) |
| 010-6239-4696 | 박태문 | KCC | (주)케이씨씨 전주3공장 |

아래 둘은 **거래처 자체가 중복**으로 보입니다. 합칠지 알려 주시면 처리하겠습니다.

### (c) 연락처 후보 10명 — 같은 회사입니까

설정 → 휴대폰 연락처 가져오기에서 같은 파일을 올리면 후보로 뜹니다.

```
KPX 그린케미칼 이지경 과장      → 그린케미칼 주식회사     ?
다모라 임승택 부장             → (주)아이마켓코리아      ?
국도화학 첨단소재 조규진 팀장   → 국도첨단소재 주식회사   ?
세스코 씨비티 백기훈 책임매니저 → (주)씨비티            ?
HP&C 에이치피앤씨 오송공장 안영진 차장 → (주)에이치피앤씨 오송공장 ?
와이파인텍 김창수 팀장          → 파인텍     ← 다른 회사로 보입니다
오빌이앤씨 김대현 부장          → 주식회사 이앤씨  ← 다른 회사로 보입니다
```

### (d) 지울지 판단이 필요한 시험용 자료

- 견적서 `Q-20260815-01` (가상업체상사) — 시험용이면 지울까요? 딸린 영업기회도 함께 정리합니다.
- 담당자 `Test Rep`, `박상현 담당아님`, `대풍 밸브문의` — 예전에 손으로 넣으신 것 같습니다.
- 품목 `신규 계약`, `테스트 제품 E2E` — 실제 품목이 아니면 지울까요?

### (e) KPI 표의 영어 지표명

`Overall` · `Weight` · `Target` · `Annual Forecast` 는 회사 공식 기준표를
그대로 옮긴 것이라 두었습니다. 한글로 바꿀까요?

---

## 이미 끝난 것 (참고)

- ✅ `pending_role.sql` 실행 — 새 계정은 승인 대기로 들어옵니다
- ✅ `neals@ind-crm.com` 계정 정리
- ✅ `deals.sql` · `sales_aggregates.sql` · `quotes_and_orders.sql` 실행
- ✅ 크롬 확장 설치 — 제가 화면을 직접 보며 작업합니다
