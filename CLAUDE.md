# CLAUDE.md

React + Vite + Supabase 기반 B2B 영업 CRM (Xavian CRM). Capacitor로 iOS/Android 패키징, Vercel 배포.

> 에이전트 운영 원칙(3-Layer Architecture, self-annealing)은 `directives/Agent.md`에 있다. **작업 시작 전 반드시 읽을 것.**
> 코드 컨벤션(Supabase 데이터 정제, 백지화면 방지 Guard Clause, 에러 처리)은 `.cursorrules`에 있다.
> 디자인 토큰/색상/타이포그래피는 `DESIGN.md`.

## 명령어

```bash
npm run dev            # Vite dev server, port 5173 (strictPort)
npm run build          # 프로덕션 빌드
npm run test:unit      # 매출 추정 엔진 회귀 테스트 (node --test, 의존성 없음)
npm run test:e2e       # Playwright E2E (tests/sanity.spec.ts)
npm run test:e2e:headed
```

> `npm run build`가 esbuild 플랫폼 에러("installed esbuild for another platform")로 실패하면
> `node_modules/@esbuild/win32-x64/`에 `esbuild.exe`가 빠진 것이다(package.json/README만 존재).
> `npm rebuild esbuild`는 듣지 않는다. 해당 폴더를 지우고 다시 받아야 한다:
>
> ```bash
> rm -rf node_modules/@esbuild/win32-x64 && npm install @esbuild/win32-x64@0.21.5 --no-save --force
> ```

업로드 UI는 **Settings 페이지의 BULK DATA OPERATIONS 패널**에 있다 (`/sales`가 아니다).
브라우저 확인용 dev 서버 설정은 `.claude/launch.json`(`crm-dev`, 포트 5173).

## 브랜드 — 회사 e카탈로그가 기준이다

전문 디자이너가 만든 `IND_eCatalog_2026.pdf`에서 값을 뽑아 옮겼다. 규격은 `DESIGN.md`,
구현은 `src/index.css`의 `--ind-*` 토큰. **문서·화면·PPT가 같은 값을 써야 한 회사가
만든 것처럼 보인다.**

- 초록 `#007538` / 먹색 `#3e3a39`(순검정 아님) / 헤어라인 `#dcdddd` / 패널 `#f2f3f3`
- 한글 나눔스퀘어 네오 + 영문·숫자 Montserrat. `public/fonts/`에 **직접 담았다** —
  CDN에 기대면 인쇄 시 글꼴이 안 와서 견적서가 다른 모양으로 나간다.
- 옅은 회색 `#9fa0a0`은 **캡션 전용**이다. 흰 배경에서 2.6:1이라 본문·라벨에 쓰면
  종이에서 안 읽힌다 (실제로 견적서 라벨에 썼다가 `#595757`로 되돌렸다).
- 표는 **초록 머리 + 흰 글씨 + 줄무늬**. 칸마다 검은 실선을 두르지 않는다
  (관공서 양식처럼 보인다). 근거는 카탈로그 p3 적재용량 비교표.

적용 상태: 견적서·발주서 · CRM 화면 · 로그인/요금 페이지 · PPT 템플릿 완료.

**PPT 같은 문서는 채팅 탭에서 만든다.** 그쪽은 이 저장소를 못 보므로 `brand/`에
자족 파일을 두었다 — `IND_BRAND_KIT.md`(색·서체·판형·패턴 전부)와
`IND_프레젠테이션_템플릿.pptx`(8장). 대화에 첨부하면 같은 결과가 나온다.
템플릿 재생성은 `node execution/make_ppt_template.mjs` (pptxgenjs는 `--no-save`로 받는다).

**PPT·Word 글꼴은 `NanumSquare` + `Arial`.** 파일에 적은 글꼴 이름은 여는 사람의
PowerPoint가 그린다 — 나눔스퀘어 네오는 대부분 안 깔려 있어 지정하면 엉뚱하게
대체된다. 네오의 전 세대인 `NanumSquare`는 한국 윈도우에 흔하고 생김새가 거의 같다.

`--accent`가 Oracle 레드(#c74634)에서 초록(#007538)으로 바뀌었다. 로그인·요금
페이지에 박혀 있던 보라 #833CF6 55곳도 함께 교체했다. **일괄 변경 후 대비비를
브라우저에서 재서 확인했다** — `/login` 실패 0, `/pricing` 실패 0.
강조가 초록이 되면서 '성공' 초록과 겹쳐 `--success`를 `#146b46`으로 눌렀다.

PDF에서 값을 다시 뽑아야 하면 PyMuPDF로 `page.get_text("dict")`의 span 색·크기와
`page.get_drawings()`의 채움색을 세면 된다. 저장소에는 남기지 않았다(일회성).

## 디자인 시스템

**`src/index.css`가 단일 기준이다.** `DESIGN.md` 스펙(Indigo Purple `#833CF6`, 8px radius, Inter)을 구현한다.
예전엔 다크 이머럴드(`:root`) / Oracle 레드(tailwind `oem-*`) / 대시보드 레드(`.dashboard-light`) /
DESIGN.md 보라 — 네 가지가 뒤섞여 어느 것도 완성되지 않은 상태였다. 변수 **이름은 유지하고 값만** 브랜드에
맞춰 전체를 한 번에 정렬했으므로, 색을 바꾸려면 `index.css`의 토큰만 고치면 된다.

지켜야 할 규칙:

- **`html`은 반드시 16px.** Tailwind의 `text-xs`(0.75rem) 등이 전부 rem 기준이라, 여기를 15px로 낮추면
  앱 전체 글자가 조용히 6% 작아진다(`text-xs`가 11.3px이 됨). 본문 크기는 `body`에서 조정한다.
- **하드코딩 색상 금지.** `bg-[#1E1E1E]`, `text-white`, `text-gray-300` 같은 다크 테마 잔재를 밝은 배경에
  남기면 글씨가 보이지 않는다. `var(--text-primary)` 등 토큰을 쓸 것.
- **터치 영역 44px.** 모바일에서 `.tb-btn`·`.rowbtn`·입력칸에 전역으로 적용된다.
  예전에는 툴바 버튼이 26px이라 전 목록 화면이 이 규칙을 어기고 있었다.
  (원래 문구) `index.css`가 아이콘 전용 버튼(`button:has(> svg:only-child)`)과 모바일 입력칸을
  전역으로 보정한다. 개별 컴포넌트에서는 `.icon-btn` / `min-h-tap`을 쓴다.
- **입력칸 전역 규칙의 우선순위를 건드리지 말 것.** `input, select`는 요소 선택자
  하나로 둔다(0,0,0,1). 미묘한 지점이다:
  `input:not([type=checkbox]):not([type=radio])`(0,0,2,1)로 쓰면 Tailwind의
  `pl-10`(0,0,1,0)을 이겨서 로그인 화면처럼 아이콘을 겹쳐 놓은 칸의 왼쪽 여백이
  사라진다(아이콘과 글자가 겹쳤다). 반대로 `:where()`로 0을 만들면 Tailwind
  리셋(`input {padding:0}`)에 밀려 조밀한 입력칸이 통째로 풀린다.
  체크박스·라디오만 속성 선택자로 되돌린다.
- **모바일 입력칸은 16px 폰트.** 16px 미만이면 iOS 사파리가 포커스 시 화면을 자동 확대한다.
  전역 규칙으로 처리되어 있으니 컴포넌트에서 `text-sm` 등으로 덮어쓰지 말 것.
- **목록은 데스크톱=표 / 모바일=카드.** 단 **마크업을 두 벌 만들지 않는다.**
  `table.dgrid` 하나를 두고 768px 이하에서 CSS가 카드로 접는다
  (`display:block` + `td::before{content:attr(data-label)}`).
  칸 이름은 `useTableLabels`가 `<thead>`를 읽어 심어 주므로 페이지마다 적지 않는다 —
  목록 화면이 7개이고 열이 조건부로 늘고 줄어서, 손으로 적으면 열을 바꿀 때마다
  두 군데를 맞춰야 하고 한쪽을 잊으면 엉뚱한 이름이 붙는다.
  (예전 문서에 적혀 있던 `.data-card` 유틸과 `hidden md:block` 쌍은 실제로는
  어느 화면에도 없었다. 표만 있었고 모바일에서 가로로 밀어 봐야 했다.)
- 행 액션 버튼에 `opacity-0 group-hover:opacity-100`을 쓰지 말 것. 터치 기기엔 hover가 없어 버튼이
  아예 보이지 않는다.
- **Tailwind 색상 클래스를 쓰기 전에 `tailwind.config.js`에 정의되어 있는지 확인할 것.** `oem-blue`가
  17개 파일에서 123번 쓰이는데 설정에 없어 전부 무효였다. 다크 테마 시절엔 티가 안 났지만 배경이
  밝아지자 `bg-oem-blue text-white`가 '투명 배경 + 흰 글씨'가 되어 텍스트가 사라졌다.

검증은 브라우저에서 수치로 한다(스크린샷 없이도 가능): 가로 오버플로, 44px 미만 터치 영역,
12px 미만 글씨, 배경/글자 대비비. 특히 **색을 일괄 변경한 뒤에는 대비비 검사를 반드시 돌릴 것** —
배경만 밝게 바뀌고 흰 글씨가 남으면 텍스트가 사라진다.

## execution/ 스크립트는 서비스 롤 키가 필요하다

RLS를 닫은 뒤로 anon 키로는 아무것도 못 읽는다. 그런데 **RLS 거부는 오류가
아니라 빈 결과**로 오기 때문에, 스크립트가 *"고칠 게 없습니다"* 라고 조용히
끝나 버린다 — 아무 일도 안 했는데 다 된 것처럼 보이는 가장 위험한 실패다.
(`repair_orphan_sales.mjs`가 "복구할 게 없습니다"라고 하면 그대로 믿게 된다.)

그래서 **접속을 `execution/_supabase.mjs`로 모았다.** `connect()`가
서비스 롤 키를 우선 쓰고, 읽히는지 실제로 찔러 본 뒤 0행이면 **바로 멈춘다.**

```
# .env.local (gitignore 대상)
SUPABASE_SERVICE_ROLE_KEY=eyJ...
```

Supabase > Project Settings > API > service_role.
**`VITE_` 접두어를 붙이면 안 된다** — 붙이면 브라우저 번들에 박혀 배포된다.

새 스크립트를 만들 때도 `createClient`를 직접 부르지 말고 `connect()`를 쓸 것.

## 알려진 손상 파일

`src/components/SalesCalendar.jsx`는 한글이 깨져(U+FFFD 85개) 문법 오류가 있는 상태다.
**어디에서도 import되지 않아** 빌드는 통과한다. 되살리려면 인코딩부터 복구해야 한다.

## 아키텍처

**3-Layer** (`.cursorrules` 참고)
- `directives/` — SOP 문서. `execution/` — 유지보수 스크립트(.mjs) 및 `execution/sql/` 마이그레이션. `src/` — React 앱. 루트 — 빌드 설정(이동 금지).

**상태 관리는 Context 중심. Redux 없음.**
- `src/contexts/DataContext.jsx` (1900줄) — 앱의 데이터 허브. clients/sales/activities/products를 Supabase에서 페이지네이션으로 전량 로드하고, `dashboardStats`(YoY, 월별 트렌드, Top/성장 고객, 휴면 고객)를 **클라이언트 사이드에서** 미리 계산해 내려준다. 대시보드 지표를 고칠 일이 있으면 대부분 여기 60~396행이다.
- `src/contexts/AuthContext.jsx` — Supabase Auth. `src/contexts/I18nContext.jsx` — ko/en. `BackgroundTaskContext` — 업로드 등 백그라운드 작업.
- `src/hooks/useDashboardData.js` — 담당자(sales_rep)별 내 실적. `SALES_REP_OPTIONS`에 영업사원 이름이 **하드코딩**되어 있고 이메일→한글이름 매핑도 여기 있다.

**라우팅**: `src/App.jsx`. `/`(Dashboard), `/clients/:id`, `/sales`, `/pipeline`, `/map`, `/calendar`, `/order-entry` 등은 `ProtectedRoute` 하위. `/landing`, `/pricing`, `/login`은 공개.

**외부 연동**: Supabase(DB/Auth/RLS), Google Gemini(`src/services/geminiService.js`, `aiSalesCoach.js`), Google Maps(`/map`), FullCalendar, Tesseract.js(명함 OCR), xlsx(엑셀 업로드/내보내기).

## 데이터 모델 주의사항

스키마는 `execution/sql/supabase_schema.sql`.

- `sales`: `client_id`, `sale_date`(DATE), `total_amount`(NUMERIC), `items`(JSONB)
- **snake_case(DB) ↔ camelCase(프론트) 혼용이 코드 전반에 존재한다.** 거의 모든 집계 코드가 `sale.total_amount ?? sale.totalAmount`, `sale.sale_date || sale.date || sale.created_at` 식으로 방어적으로 읽는다. 새 코드도 같은 패턴을 따를 것.
- 1000행 초과 조회는 `.range()` 페이지네이션 필수 (Supabase 기본 limit).
  **정렬 기준이 유일해야 한다 — `.order()`를 주는 것만으로는 부족하다.**

### 정렬이 유일하지 않으면 행이 중복·누락된다 (2026-08-15 실제 사고)

매출은 `sale_date`로 정렬하는데 같은 날짜가 수십 건이다. 값이 같은 행의 순서는
정해져 있지 않아서, 1,000행씩 끊어 받으면 **페이지 경계에서 어떤 행은 두 번 오고
어떤 행은 아예 안 온다.**

실측(브라우저, `deleted_at is null` 포함): 15,221행을 받았는데 고유 id는
**15,194개**였다 - 27행 중복, 27행 누락. 매출 합계가 **5,125,000원 많게** 나왔다.
같은 조회에서 `deleted_at` 조건만 빼면 우연히 맞는다(계획이 바뀐다).
**그래서 조용하다** - 오류도 없고 행 수도 15,221로 맞아 보인다.

화면에 이렇게 나타났다:

| 자리 | 틀린 값 | 맞는 값 |
|---|---|---|
| 올해 누적 매출 | 97억 3,923만원 | **97억 3,605만원** |
| KPI 부문기여 | 59% | **58%** |
| KPI 수익성 달성율 | 108% | **107%** |
| 영업 코치 '매출이 꺾인 곳' | 4곳 | **3곳** |
| (주)삼두 최근 3개월 | 372만원 (33% 감소) | **186만원 (66% 감소)** |

스타코스는 중복된 주문 때문에 **없는 하락을 만들어** 코치 목록에 올라 있었다.
숫자만 틀린 것이 아니라 **틀린 조언을 하고 있었다.**

`fetchAllRecords`가 `orderCol`이 `id`가 아니면 **`id`를 마지막 정렬 기준으로
더한다.** 순서가 유일해지면 경계가 흔들리지 않는다. 새 조회를 쓸 때도 같은 규칙을
지킬 것 - `company`·`name`·`created_at` 전부 유일하지 않다.

## 매출 일괄등록 — 입력 수단은 여러 개, 저장 경로는 하나

**매출을 쓰는 경로는 `src/hooks/useSalesImport.js` 하나뿐이다.** 엑셀·ERP 스크린샷·텔레그램
받은 항목이 전부 이 훅을 거친다. 새 입력 수단을 붙일 때도 반드시 여기로 넣을 것 —
대사를 우회하는 경로를 만들면 2026-08-05처럼 중복이 쌓인다(2,835건).

훅에 넘기는 행 형식: `{ clientName, sale_date, item_name, quantity, unitPrice, notes }`.
`totalAmount`는 훅이 `quantity × unitPrice`로 다시 계산한다(판독이 합계 칸을 잘못 읽어도 대사가 어긋나지 않게).

## 엑셀 일괄등록 (매출)

`src/components/SalesExcelUpload.jsx`는 파싱만 하고 나머지는 `useSalesImport`에 넘긴다. 처리 순서:

1. 엑셀 파싱 → 2. 거래처명 **퍼지 매칭**(`buildClientKeys`: ㈜/(주)/주식회사/공백/괄호 제거 후 소문자화)
→ 3. **미매칭 업체를 `registerMissingClients`로 먼저 생성** → 4. **대사**(`reconcileSales`)
→ 5. 미리보기 확인 → 6. `applySalesReconciliation`(삭제 → 수정 → 등록)

### 대사(Reconciliation) — `src/utils/salesReconciler.js`

같은 기간을 다시 올렸을 때 **중복을 만들지 않으면서 ERP에서 수정된 금액을 반영**하기 위한 순수 함수.
단순 중복 제거가 아니라 "엑셀 기준으로 해당 날짜를 맞추는" 방식이다.

- **대상 범위는 엑셀에 등장하는 날짜뿐이다.** 그 외 날짜의 매출은 절대 건드리지 않는다.
- 매칭 단계(앞에서 짝을 찾으면 뒤는 보지 않음, 한 행은 한 번만 소비):
  1. 날짜+거래처+품목+수량+단가+금액 → 유지
  2. 날짜+거래처+품목+수량 → 금액 수정
  3. 날짜+거래처+품목 → 수량·금액 수정
  4. 짝 없는 엑셀 행 → 신규 등록 / 짝 없는 기존 행 → **삭제 후보**
- 거래처가 비어있던 행(`client_id = null`)은 어느 단계에서도 매칭되지 않아 삭제 후보로 잡힌다.
  이 때문에 상반기 재업로드만으로 '알수없음' 잔재가 정리된다.
- **삭제는 미리보기에서 사용자가 승인해야만 실행된다.** 자동 삭제 금지 — 사용자가 명시적으로 정한 정책이다.

`npm run test:unit`의 `tests/salesReconciler.test.mjs`가 이 규칙들을 고정하고 있다. 로직 변경 시 반드시 실행.

**`addSale`의 `skipDuplicateCheck` 옵션**: `addSale`에는 "그 거래처의 그 날짜에 매출이 하나라도 있으면
행을 통째로 건너뛰는" 오래된 검사가 있다. 대사를 이미 거친 호출은 이 검사를 통과해야 하므로
`applySalesReconciliation`이 `skipDuplicateCheck: true`로 호출한다. **다른 호출부(AddSaleModal,
PipelineBoard)는 기존 동작을 유지한다.**

**3번이 날짜별 저장 루프보다 반드시 앞에 있어야 한다.** `addSale`은 날짜마다 한 번씩 호출되는데,
그 안의 거래처 자동 등록 폴백은 `clients` state 클로저를 보므로 같은 업체가 여러 날짜에 걸쳐 있으면
거래처가 중복 생성된다. 앞단에서 파일 전체를 한 번에 처리해야 한다.

**주의: 매출 행에 `clientName`을 반드시 실어 보낼 것.** `addSale`의 거래처 자동 등록 폴백은
`r.clientName`으로 동작한다. 과거에 이 필드가 누락되어 신규 업체의 매출이 `client_id = null`로
저장되고 목록에 '알수없음'으로 표시되는 버그가 있었다.

거래처를 끝내 확정하지 못한 행은 **저장하지 않고 건너뛴 뒤 사용자에게 알린다.**

`addSale`은 이제 `sales.client_name`에 업체명을 함께 저장한다. 앱은 이 컬럼을 읽지 않지만,
거래처 연결이 깨졌을 때 업체명을 되찾을 유일한 단서다. **지우지 말 것.**

### 거래처가 비어있는 매출 복구

과거 버그로 `client_id`가 비어있는 매출이 남아 있다면 `execution/repair_orphan_sales.mjs`로 복구한다.
당시 데이터에는 업체명이 저장되지 않았으므로 **원본 엑셀 파일이 필요하다.**

```bash
node execution/repair_orphan_sales.mjs <엑셀파일...>          # 미리보기 (DB 변경 없음)
node execution/repair_orphan_sales.mjs <엑셀파일...> --apply  # 실제 반영
```

날짜+품목+수량+단가로 원본 엑셀과 대조해 업체를 찾아낸다(2순위: 날짜+품목+총액). 후보가 여럿이면
자동으로 고르지 않고 '확인 필요'로 분류한다. **기본은 미리보기이며 `--apply` 없이는 아무것도 바꾸지 않는다.**

## 매출 집계는 서버가 한다

`execution/sql/sales_aggregates.sql` (한 번 실행). 브라우저가 매출 15,221행을
다 받아 더하던 것을 Postgres 뷰로 옮긴다. 거래가 3배 늘면 그대로 3배 느려지는
구조였다.

| 뷰 | 무엇 | 쓰는 곳 |
|---|---|---|
| `client_month_sales` | 거래처×월 합계 | KPI · 영업 코치 · 추이 |
| `monthly_sales` | 월별 전사 합계 | 상단 카드 |
| `client_sales_summary` | 거래처별 요약(올해/작년/최근3개월) | 목록 정렬 · 코치 |

**뷰에는 반드시 `security_invoker = true`를 준다.** 기본값이면 뷰가 소유자
권한으로 돌아 **RLS를 통째로 우회한다** — 애써 닫아 둔 접근제어에 구멍이 뚫린다.

계산은 `src/utils/salesAggregates.js`(순수 함수) + `tests/salesAggregates.test.mjs`.
조회는 `src/services/salesAggregateService.js`가 맡고, **뷰가 없으면 `null`을
돌려줘 예전 경로로 떨어진다** — 마이그레이션을 잊어도 화면이 죽지 않는다.

특히 `ytdLastYear`는 **같은 달수만** 비교한다. 올해 8월까지와 작년 12월까지를
비교하면 매년 실적이 줄어든 것처럼 나온다. 테스트가 이 규칙을 고정한다.

## 매출은 두 번에 나눠 받는다

첫 화면 전송량 6,082KB 중 **`sales`가 4,919KB(81%)**였다. 나머지가 전부 2.4초에
끝나는데 매출 때문에 3.7초까지 끌렸다(16쪽).

무게는 **품목·수량·단가 칸**에 있다. 대시보드·KPI·영업 코치가 쓰는 것은
거래처·날짜·금액 셋뿐이다. 실측: 10칸 4,740KB / 3칸 1,484KB - **3.2배**.

- `SALES_LIGHT`(3칸)로 먼저 받아 화면을 세운다.
- 품목이 필요한 화면만 `ensureSalesDetail()`을 부른다
  (매출 목록 · 거래명세서 · 거래처 상세 · 거래처 브리핑).
- 여러 화면이 동시에 불러도 조회는 한 번이다(`detailInFlight`).

**자동으로 뒤따라 받으면 안 된다.** 그렇게 해 봤더니 총 전송량이 4,919KB ->
7,858KB로 오히려 늘었다. 대시보드만 보고 나가는 날이 대부분이다.

실측: 첫 화면 6,082KB -> **2,737KB**. 숫자는 하나도 바뀌지 않았다
(위 정렬 버그를 고친 뒤 양쪽 경로가 같은 값을 낸다는 것을 화면에서 대조했다).

`id`·`created_at`을 가벼운 쪽에서 뺐다 - 둘이서만 1,455KB다. 빼면 주문 묶음이
`날짜|거래처`가 되어 같은 날 두 번 들어온 주문이 하나로 합쳐지지만,
**합계·거래처·날짜는 그대로**라 대시보드 숫자가 바뀌지 않는다. 건수가 중요한
매출 목록 화면은 상세를 받으므로 영향이 없다.

## 화면은 열 때 받는다 (코드 분할)

예전에는 전부 한 덩어리(2,315KB)라 대시보드만 보려 해도 견적서·지도·요금표까지
다 받았다. `React.lazy`로 나눠 **초기 1,046KB + 조각 34개**가 됐다.
대시보드만 미리 넣고 나머지는 그 화면으로 갈 때 받는다.
xlsx(417KB)·FullCalendar(232KB)처럼 무거운 것이 첫 화면에서 빠진 것이 크다.

## 매출 추정 엔진 (핵심 도메인 로직)

파일 3개로 구성:
- `src/utils/revenueForecastEngine.js` — 순수 계산 엔진 (v7.0). 부작용 없음. `now`를 주입받아 테스트 가능.
- `src/services/forecastService.js` — 데이터 fetch + `revenue_forecasts` 테이블 24시간 캐시.
- `src/components/RevenueForecastPanel.jsx` — 대시보드 UI. 버튼 클릭 시에만 계산(on-demand).

**임계값은 전부 `FORECAST_CONFIG`에 모여 있다. 튜닝은 반드시 거기서 한다.**
**로직을 건드리면 `npm run test:unit`을 돌린다.** `tests/revenueForecastEngine.test.mjs`는 과거에 실제로
발생했던 결함들을 고정한 회귀 테스트다 (의존성 없이 `node --test`로 동작).

### 계산 순서

1. **집계** — 최근 4년(`currentYear-3` ~ `currentYear`)의 매출을 `clientMap[client_id][year][month]` 12칸 배열로 누적.
2. **세그먼트 분류** — 고객 1명당 하나. 판정 순서가 곧 우선순위다:
   | 세그먼트 | 조건 | 예측식 |
   |---|---|---|
   | `NewThisYear` | 과거 3년 실적 전무, 올해만 거래 | 올해 YTD ÷ 경과 영업일 × 연 영업일, 3배 캡 |
   | `Churned` | 과거 피크(2년전·3년전 중 max) > 10만원 **and** 작년 ≤ 피크의 30% | 작년 ≤ 10만원이면 0, 아니면 작년 실적 유지 |
   | `HighPotential` | 작년에 거래 시작 **and** H2 > H1×1.5 **and** H2 > 100만원 | H2 영업일당 매출 연환산 × 1.1, 작년의 3배 캡 |
   | `New` | 거래 시작 2년 이내 **and** 최근 2년 활동 월수 ≤ 9 | 작년 ÷ 활동월수 × 12, **작년의 3배 캡** |
   | `Growing` | 그 외, 영업일 정규화 성장률 ≥ +10% | 작년 × (1 + min(정규화성장률, 0.3)) |
   | `Declining` | 그 외, 영업일 정규화 성장률 ≤ −10% | 작년 × (1 + max(정규화성장률, −0.2)) |
   | `Stable` | 나머지 | (2년전 + 작년) ÷ 2 |
   - 분류와 예측 배수 **모두 영업일 정규화 성장률(RPBD)** 을 쓴다. 둘을 다른 지표로 계산하면 라벨과 숫자가 어긋난다.
   - 월별 배분("시즌성")은 작년 월별 실적을 **영업일당 매출로 환산한 뒤 예측 연도 영업일로 재분배**한다.
     작년 구성비를 그대로 복사하면 설날/추석 이동분이 엉뚱한 달에 실린다
     (2026-02 영업일 17일 vs 2025-02 20일, 2025-10 18일 vs 2026-10 20일).
3. **YTD 캘리브레이션** — 모델의 연초~오늘 누계(**영업일 기준**) 대비 실제 누계 비율(`rawScale`)을 전체에 곱한다.
   **[0.8, 1.2]로 클램프**. 올해 실적이 모델 YTD의 10% 미만이면 데이터 미입력으로 보고 스케일 1.0 + `incompleteFlag`.
4. **출력 조립** — 마감된 달 = 실적 확정 / **진행 중인 달 = 확정 실적 + 잔여 영업일 예측** / 미래 = 예측.
   즉 `total_amount`는 "YTD 실적 + 잔여기간 예측"이며, 정의상 이미 확정된 매출보다 작아질 수 없다.

### 설계상 의도된 동작 (버그로 오인하기 쉬움)

- **올해 데이터가 비면 예측 총액이 작아 보인다.** 마감된 달을 실적으로 덮어쓰는 구조라, 매출 입력이
  안 된 상태면 그 달이 0으로 확정된다. 이 경우 `incompleteFlag`가 서고 UI에 "데이터 부족" 배지와
  요약문 ⚠️ 가 표시된다. **배지가 떠 있으면 숫자를 신뢰하면 안 된다.**
- `Churned`의 'Reduced' 케이스는 추가 감소 없이 작년 수준을 유지한다 (추가 감소를 가정하지 않음).
- 세그먼트 캡(3배 / +30% / −20%)과 전체 배율 클램프(0.8~1.2)는 이중 안전장치다. 급성장 고객이 많은
  해에는 예측이 구조적으로 보수적으로 나온다.

### 남은 이슈

- `getCachedForecast`에 `user_id` 필터가 없다. 이 패널은 전사 매출을 계산하므로 **캐시 공유가 의도된
  동작**이지만, `execution/sql/`에 엄격 RLS(`FIX_FORECAST_RLS_FULL.sql`)와 전체 허용 RLS
  (`FIX_RLS_RELAXED.sql`)가 둘 다 있어 어느 쪽이 적용됐는지 확인이 필요하다. 엄격 RLS가 걸려 있으면
  캐시가 사실상 동작하지 않는다(다른 사용자 행이 안 보여 매번 재계산).
- 분석 실행마다 `revenue_forecasts`에 새 행을 INSERT한다 (정리 로직 없음).
- 캐시에서 읽은 예측에는 `incompleteFlag` 컬럼이 없어, UI가 `analysis_summary`의 ⚠️ 접두어로 판단한다.

### 공휴일 데이터

`src/utils/koreanHolidays.js`의 `HOLIDAYS_BY_YEAR`는 **2023~2026년만 하드코딩**되어 있다. 등록되지 않은
연도는 주말만 제외되어 영업일이 과대 계산되므로, `getHolidays()`가 콘솔 경고를 띄우고
엔진은 `holidayDataMissing`을 반환한다. **매년 갱신 필요.**

## KPI 위젯 (평가 지표)

`src/components/KPIWidget.jsx`. 대시보드의 KPI Performance 카드. 담당자(sales_rep) 기준으로 집계한다.

**임계값은 두 상수에 모여 있다. 기준이 바뀌면 여기만 고친다.**

```js
KPI_REVENUE_QUALIFY = { HALF_YEAR: 1천만, ANNUAL: 2천만 }   // 실적 인정 기준
CHURN_RULE          = { GAP_MONTHS: 6, MIN_HISTORY_REVENUE: 1천만 }  // 단절 판정
```

### 수익성 = 매출 목표달성율 (EBITDA 대체)

원 기준표는 **EBITDA(영업이익) 18/16/15/12억**이지만 영업이익은 CRM에서 구할 수 없어
매출액으로 대체했다. **이때 억원 기준을 그대로 쓰면 안 된다** — 전사 매출이 연 125억대라
15억 기준으로는 언제나 '탁월'이 나온다.

기준표의 등급 헤더(120~/110~/100~/90~/80~) 자체가 달성율 눈금이므로, 억원이 아니라
**달성율(%)에 구간을 적용**한다 (`KPI_BANDS.revenue`).

- 실적 = 연말 예상 매출 = 올해 누계 × (연간일수 ÷ 경과일수).
  연중에 YTD를 연간 목표와 그대로 비교하면 항상 미달로 나오므로 경과일 비례 환산이 필요하다.
- 목표 = 수동 입력(`manual.revenueTarget`, 억원). 미입력 시 `DEFAULT_REVENUE_TARGET_EOK`(2026년 경영계획 **145억**).
  localStorage 입력값은 기기마다 따로 남으므로, 회사 공식 목표는 상수를 기본값으로 둔다.
- 2026-08-07 기준 실측: 올해 91.9억(219일 경과) → 연말 예상 153.2억 ÷ 목표 145억 = 106% → 양호.

참고 실적: 2023년 87.9억 / 2024년 115.6억 / 2025년 125.2억.

### 공식 기준표와의 대응 (2026년간 KPI)

항목·가중치·등급구간은 회사 공식 표를 그대로 옮겼다. `KPI_BANDS` / `getGradeInfo`가 단일 출처다.

| 항목 | 가중치 | 지표 | 산출 |
|---|---|---|---|
| 수익성 | 40 | EBITDA(영업이익) 목표달성 | **수동 입력** (CRM에 자료 없음) |
| 부문기여 | 20 | 25년대비 26년 판매상승률 | 자동 |
| 고객관리 | 15 | 기존고객 및 단절고객 편입 | 자동 |
| 신규고객 발굴 | 10 | 매출발생 기준 | 자동 |
| 정기적방문횟수 | 10 | 연간 기준 (계획 240건) | 자동 (연말 예상치로 환산) |
| 채권관리 | 5 | 연간 기준 (적을수록 좋음) | **수동 입력** |

등급: 탁월 120~ / 우수 110~ / 양호(계획) 100~ / 보통 90~ / 미흡 80~

과거에 있었던 오류들 — 같은 실수를 반복하지 말 것:

- **채권관리(가중치 5) 항목이 아예 없었다.** 가중치 합이 95점이라 총점이 실제와 달랐다.
- **등급 구간이 S≥110 / A≥100 / B≥80 / C≥60 이었다.** 한 칸씩 후해서 110%가 '탁월'로 표시됐다.
- **고객관리가 '편입 1건당 20%'였다.** 기준표상 0건은 '양호(100%)'인데 0%(미흡)로 잡혀 담당자가 크게 손해봤다.
- **정기적방문 목표가 52주×2=104건이었다.** 기준표의 계획은 연 240건이다.
- **수익성이 '전년 대비 매출'이었다.** 기준표는 EBITDA(영업이익)다. 전혀 다른 지표다.

**미입력 항목은 0으로 치지 말 것.** `bandScore`가 `null`을 돌려주고 총점 계산에서 제외한다.
`Number(null)`은 0이라 그냥 `Number()`로 받으면 '미입력'이 '미흡(80%)'이 되어 총점이 부당하게 깎인다.
수동 입력값은 `getKpiManualInputs` / `setKpiManualInput`(localStorage)로 관리한다.

최종평가는 100점을 90점으로 환산하고 2차조정자 임의평가 10점을 합산한다 — 이 환산은 화면에서 하지 않는다.

### 실적 인정 기준 (신규·편입 공통)

**반기 1천만원 또는 연 2천만원 이상.** 둘은 같은 속도이므로 하나만 넘으면 인정한다
(상반기에 1천만원을 채우면 연말까지 기다리지 않는다).

### 신규고객 발굴

**올해 처음 거래한 곳**만 신규다. **CRM 등록일(`created_at`)로 판정하면 안 된다** —
거래처 데이터를 올해 한꺼번에 입력해서 담당 40곳 전부가 '올해 등록'이었고,
2025년부터 매월 거래하던 곳까지 신규로 잡혔다(20건). 실제 매출 이력 기준으로 5건.

### 단절고객 편입

**6개월 이상 거래 공백 + 과거 누적 1천만원 이상 거래 이력**이 있어야 단절로 본다.

- 과거 실적 조건이 핵심이다. 없으면 한두 번 소액만 사고 만 곳까지 잡혀 목록이 의미를 잃는다(16곳 → 4곳).
- 공백 6개월인 이유: 2~3개월 간격으로 꾸준히 소액 주문하는 정상 거래처가 있다
  (리메카 — 3년간 17개월 주문, 누적 2,182만원). 5개월로 잡으면 이런 곳이 단절로 분류된다.
- 편입 = 단절 상태였다가 올해 다시 거래 + 실적 기준 통과.
  **올해 처음 거래한 곳은 편입에서 제외한다** (신규와 이중 계상 방지).

과거에 있었던 버그: 단절 목록을 만들 때 '최근 거래가 있는 곳'을 미리 제외해 놓고
그 목록에서 다시 '최근 거래가 있는 곳'을 찾았다. 교집합이 정의상 항상 비어
**이 KPI는 구조적으로 영원히 0건**이었다.

### KPI별 수동 제외

`src/utils/kpiCategories.js`의 `getKpiExclusions` / `toggleKpiExclusion` / `isExcludedFrom`.
localStorage에 `{ clientId: { new?: true, churn?: true } }` 형태로 저장한다.

- `new` — 자회사 파생 등으로 신규로 보기 어려운 곳
- `churn` — 폐업·상호변경으로 복구 불가능한 곳

**반드시 KPI별로 제외해야 한다.** 담당 거래처 목록(`managedClientIds`)에서 통째로 빼면
매출·부문기여 KPI의 실적까지 사라져 담당자가 손해를 본다. 자회사 파생 건은 '신규'만
아닐 뿐 매출은 정상 실적이다.

기존 `setKpiCategory`의 `'미산정'`(전체 제외)과는 별개 저장소다.

## Gemini (AI 기능)

`src/services/geminiService.js`. 모델명은 `VITE_GEMINI_MODEL`로 바꿀 수 있고 기본값은 `gemini-2.5-flash`다.
**`gemini-1.5-flash`는 단종되어 404가 난다** — 하드코딩하지 말 것.

키가 무효하거나 모델이 없는 등 **재시도해도 소용없는 오류는 재시도하지 않는다.** 한 번 확인되면
모듈 수준에서 AI 기능을 끄고(`disabledReason`) 경고를 1회만 출력한다. 이후 호출은 네트워크 없이
즉시 실패한다. 상태는 `isGeminiAvailable()` / `getGeminiDisabledReason()`으로 확인한다.
이 래치가 없으면 대시보드를 열 때마다 같은 에러가 콘솔을 수십 줄씩 채운다.

## 환경 변수

`.env` / `.env.local` (커밋 금지). Supabase URL/anon key, Google Maps API key, Gemini API key,
`VITE_GEMINI_MODEL`. 상세는 `directives/ENV_SETUP_GUIDE.md`.

> **`VITE_` 접두어가 붙은 값은 빌드 결과물에 그대로 박혀 배포된다.** `dist/assets/*.js`에서 문자열로
> 추출 가능하므로, 브라우저에 노출돼도 되는 값만 넣을 것. Supabase anon key는 RLS로 보호되므로 정상이지만,
> **Gemini 키는 노출 시 제3자가 사용자 계정으로 API를 호출할 수 있다.**
> 근본 해결은 서버(예: `api/` 서버리스 함수)에서 호출하고 키를 `VITE_` 없이 두는 것이다.
> 과거 커밋에 키가 하드코딩된 이력이 있으므로(`git log --all -S"AIzaSy"`), 그 키들은 폐기되어야 한다.

## ERP 화면 스크린샷으로 입력

`src/components/ErpScreenshotImport.jsx` (설정 > Bulk Data Operations 아래).
붙여넣기(Ctrl+V)·끌어놓기·파일선택·휴대폰 카메라를 받는다. 최대 6장.

```
스크린샷 → /api/analyze-erp (Gemini Vision) → 화면에서 사람이 확인·수정 → 반영
```

- **판독 결과를 바로 저장하지 않는다.** 표로 띄워 고칠 수 있게 한 뒤 반영한다.
- 매출 → `useSalesImport`(대사). 같은 화면을 두 번 올려도 중복되지 않는다.
- 채권 → 연체 건수를 `manual.receivables`(KPI 채권관리)에 저장. 저장 후
  `window.dispatchEvent(new Event('kpi-manual-updated'))`로 KPI 카드에 알린다
  (localStorage는 같은 탭에서 `storage` 이벤트가 오지 않는다).
- 일정·활동 → `addActivity`. 거래처를 못 찾은 행은 저장하지 않고 알린다.

`src/services/erpVisionService.js`가 이미지 축소(최대 2000px, JPEG 0.85)와 결과 정규화를 맡는다.
**`normalizeDate`가 핵심이다** — 화면 판독은 `26.01.22`, `01/22`, `20260122`가 뒤섞여 나오는데
대사는 `YYYY-MM-DD`가 정확히 맞아야 기존 매출을 찾는다. `tests/erpVision.test.mjs`가 고정한다.

**API 키는 서버에만 둔다.** 판독은 `api/analyze-erp.js`에서 하고 `GEMINI_API_KEY`(VITE_ 없음)를 쓴다.
프론트에서 직접 부르면 키가 배포 번들에 박힌다.

## 텔레그램 봇 입력

설정 순서는 `directives/TELEGRAM_SETUP.md`. 스키마는 `execution/sql/telegram_inbox.sql`.

```
휴대폰 → 봇(api/telegram-webhook.js) → Gemini 판독 → telegram_inbox(대기)
       → 앱 '받은 항목'(InboxPanel)에서 사람이 확인 → 반영
```

- **봇은 매출을 직접 INSERT하지 않는다.** 대사 로직이 앱에 있기 때문이다. 담아두기만 한다.
- 보안 장치 두 개. 둘 다 없으면 안 된다:
  1. `TELEGRAM_WEBHOOK_SECRET` — 웹훅 주소는 공개 URL이라 비밀 토큰 검증이 없으면 누구나 쓸 수 있다.
  2. `TELEGRAM_ALLOWED_CHAT_IDS` — 봇 아이디는 검색되므로 허용 목록이 실질적인 자물쇠다.
     비어 있으면 봇은 아무것도 받지 않고 chat id만 알려준다.
- `SUPABASE_SERVICE_ROLE_KEY`는 RLS를 우회한다. **서버 환경변수에만** 둘 것.
- **카카오톡은 불가능하다.** 개인 대화를 읽는 API가 없다(카카오 정책). 채널 챗봇은
  사업자 심사가 필요하고 채널로 온 문의만 읽는다.

## 개발 서버에서 api/ 함수 실행

`vite-plugin-api-dev.js`. `vite dev`는 `api/`를 엔드포인트로 만들지 않아 예전엔
스크린샷 판독을 배포해야만 확인할 수 있었다. 이 플러그인이 `/api/*`를 해당 파일의
default export로 넘겨 준다. `.env`/`.env.local` 값을 `process.env`에 실어 주므로
`GEMINI_API_KEY` 같은 VITE_ 없는 변수도 개발 중에 동작한다.

## 일일업무보고서 -> 활동 반영

`execution/import_daily_report.mjs`. 시트 하나가 하루인 회사 양식을 읽어 `activities`에 넣는다.

```bash
node execution/import_daily_report.mjs "<보고서.xls>"                      # 미리보기
node execution/import_daily_report.mjs "<보고서.xls>" --apply --create-clients
```

- **'금일 영업 계획'은 반영하지 않는다.** 아직 일어나지 않은 일이고, 실제로 다녀오면
  다음 날 시트에 방문기록으로 다시 나온다. 같이 넣으면 이중 계상된다.
- 중복 방지는 `(client_id, activity_date)` 기준. 같은 보고서를 다시 올려도 늘지 않는다.
- `ALIASES` 표가 핵심이다. 보고서 표기와 CRM 거래처명이 달라서(`KP한석유화` ↔
  `케이피한석유화 주식회사`, `KCC 전주공장` ↔ `KCC`) 그냥 두면 같은 회사가 하나 더 만들어진다.
  미매칭으로 뜨는데 실은 CRM에 있는 곳이면 이 표에 추가할 것.
- 여러 회사를 한 칸에 적은 행(`성진실업 인지산업 남양화학`)과 `사무실` 같은 항목은 건너뛴다.
- 새로 만드는 거래처는 `status: '잠재고객'` + `sales_rep`(보고서 작성자). **`clients`에 `notes`
  컬럼은 없다.**

### KPI 정기적방문횟수와의 관계

`KPIWidget`은 **담당 거래처(`sales_rep`)의 `미팅`/`방문` 활동만** 센다. 그래서:

- `유선` 방문은 `전화`로 저장되어 KPI에서 빠진다. '정기적방문'이므로 의도된 동작이다.
- **`sales_rep`이 비어 있는 거래처의 방문은 KPI에 잡히지 않는다.** 2026-08-08 기준
  미팅/방문 137건 중 17건이 이 이유로 빠졌다. 담당을 지정하면 잡히지만, 매출이 있는
  거래처를 담당으로 넣으면 부문기여·신규고객·단절고객 KPI 숫자도 함께 바뀐다.

## 외상매출금 대장 분석

`execution/analyze_receivables.mjs` (읽기 전용).

```bash
node execution/analyze_receivables.mjs "<외상매출금.xlsx>"
```

- **대장 금액은 부가세 포함, CRM은 공급가액이다.** 그대로 비교하면 CRM이 늘 9% 적어 보인다.
  `÷1.1` 해야 맞는다. 2024-12 ~ 2026-05 전체 대조 결과 일치율 99.9%였다.
- 월 열 위치는 헤더에서 찾는다. 하드코딩하면 다음 달에 열이 밀려 깨진다.
- '지연' 표시 건수가 KPI 채권관리에 넣을 숫자다. localStorage 값이라
  **스크립트가 넣을 수 없고 화면에서 직접 입력해야 한다.**

## 업무일지 스크린샷 -> 활동

`ErpScreenshotImport`의 `일일업무보고서` 종류. 엑셀 일괄반영(`import_daily_report.mjs`)과
같은 규칙을 화면에서 한 장씩 처리하는 경로다.

- **'금일 영업 계획'은 판독 단계에서 뺀다.** 프롬프트(`api/analyze-erp.js`의 `daily_report`)에
  명시되어 있다. 아직 다녀오지 않은 계획이고, 실제로 다녀오면 다음 날 일지에 방문기록으로
  다시 나와 이중 계상된다.
- 중복 방지는 `(client_id, activity_date)`. 같은 일지를 두 번 찍어 올려도 활동이 늘지 않는다.
  같은 거래처를 하루에 두 번 방문한 경우 두 번째는 건너뛴다 — 양식이 하루 한 줄이라 감수한다.
- 방문시간이 `유선`이면 `전화`, 아니면 `미팅`으로 저장한다. **KPI 정기적방문횟수는 미팅/방문만
  세므로** 이 구분이 곧 KPI 숫자다.

### 거래처명 대응표는 두 군데에 있다

`src/utils/clientAliases.js`(앱) 와 `execution/import_daily_report.mjs`의 `ALIASES`(스크립트).
스크립트는 Node에서 돌아 앱 모듈을 직접 읽지 못한다. **한쪽을 고치면 다른 쪽도 고칠 것.**
`nameCandidates`가 괄호(`아모레퍼시픽 (오산)` → `(주)아모레퍼시픽`)와 공장 접미사
(`KCC 전주공장` → `KCC`)까지 훑는다.

## 채권관리 탭 (`/receivables`)

`src/pages/Receivables.jsx`. 결제가 밀린 순서로 거래처를 세운다.
데이터는 `receivables` 테이블(월 스냅샷)이고 `analyze_receivables.mjs --apply`가 채운다.

준비(최초 1회): `execution/sql/receivables.sql`을 Supabase SQL Editor에서 실행.
**이 DDL은 anon 키로 못 한다** — CLI·액세스 토큰·DB 비밀번호가 없으면 사람이 직접 해야 한다.

그 다음부터는 화면의 **대장 올리기** 버튼으로 엑셀을 올리면 된다(터미널 불필요).
스크립트 경로도 그대로 쓸 수 있다: `analyze_receivables.mjs "<파일>" --apply`.
같은 기준월을 다시 올리면 덮어쓴다.

**판독·계산은 `src/utils/receivablesLedger.js` 하나뿐이다.** 화면 업로드와 스크립트가
같은 코드를 쓴다. 결과가 어긋나면 안 되기 때문이다 (`tests/receivablesLedger.test.mjs` 12건이 고정).
열 위치는 헤더에서 찾는다 — 하드코딩하면 달이 하나 늘 때 뒤가 밀려 조용히 틀린 값을 읽는다.

### 연체 판정 — 대장의 '지연' 메모를 쓰지 않는다

메모는 **108곳 중 10곳에만** 적혀 있어 정렬 기준이 되지 못한다. 대신 계산한다:

- **경과월(`aging_months`)** — 잔액을 기준월 매출부터 거꾸로 배분(FIFO)해서
  가장 오래된 미수분이 몇 개월 전 매출인지.
- **연체금액(`overdue_amount`)** — 당월 매출을 넘어선 잔액. 실제로 밀린 돈.

정상 거래처는 `수금 = 전월 매출`, `잔액 = 당월 매출` 형태로 익월 결제가 지켜진다
(바커케미칼·그린케미칼이 그렇다). 그래서 **경과 0개월은 연체가 아니다.**

2026-05 기준 실측: 정상 78곳 / 1개월 19곳 / 2개월 5곳 / 3개월 이상 5곳,
연체 37곳 5.47억. 메모가 놓친 건들이 드러났다 — 금잔디 14개월, 한국기능성화장품연구센터 9개월,
한경티이씨 4개월(2,965만원).

메모는 참고용으로 '대장 메모' 열에 그대로 보여준다. 계산값과 어긋나는 경우가 있다
(인터코스코리아: 메모 '2개월' vs 계산 1개월).

총 미수금은 **음수(선수금)까지 합산**한다. 그래야 대장의 합계행과 맞아떨어진다
(2026-05: 1,889,368,834원으로 일치).

### 화면

기본 필터는 '연체만', 기본 정렬은 경과월 desc → 같으면 연체금액 desc.
헤더를 누르면 정렬이 바뀐다. 'KPI 채권관리에 N건 저장' 버튼이 연체 업체 수를
localStorage에 넣고 `kpi-manual-updated` 이벤트를 쏜다.

### 채권이 아닌 건 제외하기

대장에 미수로 잡혀 있어도 실제 채권이 아닌 경우가 있다.
예) 한국기능성화장품연구센터 — 선입금 후 나중에 물건만 가져간 건을 회계팀이 미수로 잘못 잡았다.

**행을 지우면 안 된다.** 회계 장부가 고쳐지기 전까지 다음 달 대장에도 그대로 나오므로
지워봐야 다음 업로드에서 되살아나고, 왜 뺐는지도 남지 않는다.
대신 `excluded` / `exclusion_reason`을 표시한다 (`execution/sql/receivables_exclusions.sql`).

- 화면의 행 오른쪽 눈 아이콘으로 사유를 적어 제외한다. 되돌리기도 같은 자리.
- 제외된 건은 **총 미수금·연체 건수·3개월 이상에서 모두 빠진다.** KPI로 보내는 숫자도 마찬가지다.
- 새 달을 올릴 때 **같은 거래처명이면 제외 표시를 물려받는다.** 화면 업로드와 스크립트 양쪽에 있다.
- `excluded` 열이 없어도(마이그레이션 전) 업로드·조회는 그대로 동작한다.

## 일정 달력 (대시보드 중앙)

`src/components/ScheduleCalendar.jsx` + `schedules` 테이블
(`execution/sql/schedules_and_inbox.sql`).

**`activities`와 반드시 따로 둔다.** `activities`는 '다녀온 기록'이고 KPI
정기적방문횟수의 근거다. 아직 가지 않은 계획을 같은 표에 넣으면 방문 실적이
부풀려진다 (일일업무보고서의 '금일 영업 계획'을 넣지 않는 것과 같은 이유).

- 월 그리드는 6주(42칸) 고정. 달마다 높이가 튀지 않게.
- 날짜는 `ymd()`로 **로컬 기준** 문자열을 만든다. `toISOString()`은 UTC라 하루 밀린다.
- 720px 미만에서는 달력과 목록을 위아래로 쌓는다(`.sched-body`). 2단이면 최소 폭이
  500px이라 375px 화면에서 가로 스크롤이 생긴다.

## 텔레그램 — 보낸 것을 알아서 갈라 넣기

`api/telegram-webhook.js`. 하나의 창구로 받아 종류를 판단하고 나눈다.

| intent | 어디로 | 시점 |
|---|---|---|
| `schedule` | `schedules` | 바로 |
| `activity` | `activities` | 바로 (같은 거래처·같은 날은 건너뜀) |
| `sales` | `telegram_inbox` | 앱에서 확인 후 |
| `receivables` | `telegram_inbox` | 앱에서 확인 후 |
| `question` | — | `/today`·`/week` 답장 |
| `memo` | `telegram_inbox` | — |

### 설정을 하나로 줄인 이유

처음엔 Vercel 환경변수 6개 + 터미널 명령을 요구했는데, 사용자가 "뭔 말인지 모르겠다"고 했다.
**설정 단계가 많으면 기능이 없는 것과 같다.** 그래서 전부 없앴다:

- 비밀 토큰 → `deriveSecret(botToken)`으로 계산. 사람이 정해 두 군데 옮겨 적을 일이 없다.
  (`api/telegram-setup.js`와 `api/telegram-webhook.js`가 같은 함수를 쓴다.)
- 허용 목록 → `bot_allowlist` 표. **첫 `/start`가 스스로 등록**하고 그 뒤로는 RLS로 잠긴다
  (`with check ((select count(*) from bot_allowlist) = 0)`).
- Gemini/DB 키 → 이미 있는 `VITE_` 값으로 폴백.
- 웹훅 등록 → `/api/telegram-setup` 주소를 열면 서버가 자기 자신을 등록한다. 터미널 불필요.
  받는 값이 없고 자기 배포 주소로만 설정하므로 남이 눌러도 무해하다.

**남는 것은 `TELEGRAM_BOT_TOKEN` 하나뿐이다.**

- **매출·채권만 바로 넣지 않는다.** 대사 로직이 앱에 있기 때문이다.
- 서버는 UTC로 돈다. `kstToday()`로 한국 날짜를 만들어 프롬프트에 넣어야
  '내일'·'다음주 화요일'이 맞게 풀린다. 저장할 때도 `+09:00`을 붙인다.
- 거래처 매칭은 `src/utils/clientAliases.js`를 **서버에서도 import**해 쓴다.
  앱과 기준이 갈리면 같은 회사가 새로 만들어진다.

실측(2026-08-08 금요일 기준) — '내일 오후 2시' → 2026-08-09 14:00,
'다음주 화요일 10시' → 2026-08-12 10:00, '유선으로' → `전화` 유형으로 정확히 분류됐다.

## KPI 카드 펼침은 표 아래 전체 폭에

`KPIWidget`의 `renderDetail(item)`. 예전에는 카드 안에서 펼쳤는데, 6칸 그리드라
칸 폭이 190px밖에 안 돼 거래처 목록 글자가 겹치고 제외 버튼이 칸 밖으로 나갔다.
지금은 카드에 '아래에서 자세히 보기 ▼'만 두고 그리드 아래에 전체 폭(1216px)으로 그린다.

### 수익성 카드 표시값

영업사원이 매일 보는 건 달성율(%)이 아니라 매출액이다. 그래서 카드에는
**올해 누적 매출액**을 크게, 그 아래 **작년 동기 대비 증감률**을 보여준다.
등급 계산은 그대로 `연말 예상 ÷ 목표(145억)` 달성율을 쓰고, 그 값은 '달성율' 줄에 남긴다.

## 대시보드 구성 (2026-08 개편)

위에서부터: 요약 카드 4개 → **일정 달력 + 영업 코치** → KPI → 매출 추정 → 최근 활동.

매일 여는 화면이라 **달력을 KPI보다 위로** 올렸다. KPI 카드는 여백을 줄여
모바일에서 2열로 들어간다.

없앤 것:
- **Weekly Revenue Trend** — 매출 추정 패널과 겹치고, 실적이 0이면 난수를 그리고 있었다.
- **Priority Issues / Key Accounts** — 쓰지 않는 화면이었다.
- **AIHeadline** — "최근 7일 활동 N건" 한 줄이 전부라 코치 구실을 못 했다.

### 영업 코치 (`src/components/SalesCoach.jsx`)

거래처별 **매출 흐름과 접점 이력을 함께** 보고 우선순위를 매긴다. 네 갈래:

| 갈래 | 조건 | 정렬 |
|---|---|---|
| 매출이 꺾인 곳 | 직전 3개월 500만원↑ **and** 최근 3개월이 그보다 30%↓ | 빠진 금액 |
| 연락이 끊긴 곳 | 누적 1천만원↑ **and** 6개월 무거래 **and** 3개월 무접점 | 누적 매출 |
| 크는데 방치된 곳 | 최근 3개월이 직전보다 20%↑ **and** 90일 무접점 | 늘어난 금액 |
| 아직 첫 거래가 없는 곳 | 매출 이력 0 **and** 6개월 내 방문 | 최근 방문순 |

**규칙 기반이다. 문장 생성 모델을 쓰지 않는다.** 근거 숫자를 그대로 보여줄 수 있어야
움직일 수 있는데, 모델은 숫자를 지어낼 수 있다. 판단은 코드가 하고 사람이 검증한다.
'연락이 끊긴 곳'의 1천만원·6개월은 KPI 단절 기준(`CHURN_RULE`)과 눈높이를 맞춘 것이다.

### 달력의 공휴일

`koreanHolidays.js`의 `HOLIDAYS_BY_YEAR`를 그대로 쓴다(매출 추정 엔진과 같은 자료).
공휴일은 일요일과 같은 빨간색 + 이름 표시. **2023~2026만 있으므로 매년 갱신해야 하고**,
없는 해는 상단에 '공휴일 미등록' 경고가 뜬다.

### 상단 카드가 0으로 나오던 문제

`dashboardStats`(DataContext)와 `rawSalesData`(useDashboardData)가 **각각 따로**
전체 매출을 조회한다. 앞쪽이 비면 상단 카드만 전부 0이 된다(KPI는 멀쩡한데).
지금은 비었을 때 `rawSalesData`로 채우도록 폴백을 뒀다.
**근본 해결은 두 벌 조회를 하나로 합치는 것이다** — 1.5만 행을 두 번 읽고 있다.

## 휴지통 · 변경 이력

`execution/sql/soft_delete_and_audit.sql`을 한 번 실행해야 한다.
실행 전에는 설정 화면의 휴지통이 '준비되지 않았습니다'를 띄우고, 조회는
`deleted_at` 조건 없이 한 번 더 시도해 그대로 동작한다 (앱이 먼저 죽으면 안 된다).

**지우기는 '표시'다.** `deleteClient`는 예전에 담당자·활동·매출을 **진짜로**
지우고 거래처를 지웠다. 거래처 하나를 잘못 누르면 그 회사의 매출이 통째로
사라지고 되돌릴 방법이 없었다. 지금은 `deleted_at`만 채운다.

- **딸린 자료도 함께 표시한다.** 거래처만 되살리고 매출이 안 돌아오면 의미가 없다.
- `fetchAllRecords`가 `deleted_at is null`로 거른다. 칸이 없으면(마이그레이션 전)
  조건 없이 재시도하고 경고를 한 번만 띄운다.
- 되살리기는 설정 > **휴지통 · 변경 이력**에서. 관리자만 누를 수 있다.

**변경 이력은 DB 트리거가 쓴다.** 앱을 거치지 않은 변경(스크립트·SQL 편집기)도
남기려면 트리거여야 한다. 바뀐 칸만 저장한다 — 통째로 넣으면 이력 표가 원본만큼
커진다. `updated_at`만 바뀐 것은 변경으로 치지 않는다.

**매출·활동은 삭제만 기록한다.** 엑셀 일괄등록 한 번에 수천 행이 들어오는데
INSERT/UPDATE까지 남기면 이력이 매출보다 커지고 업로드가 느려진다.
정작 위험한 것은 삭제다.

이력 기록이 실패해도 업무는 막지 않는다 (트리거가 예외를 삼키고 warning만 낸다).
이력 표는 **읽기만** 열려 있다 — 고치거나 지울 수 있으면 이력이 아니다.

## 로그인 · 역할 · 접근제어

`execution/sql/auth_and_roles.sql`을 Supabase SQL Editor에서 **한 번 실행해야** 한다.
**몇 번을 다시 돌려도 안전하다** — 이미 있는 것은 건드리지 않고 모자란 것만 채운다.

처음 돌렸을 때 두 번 걸렸다. 같은 실수를 반복하지 말 것:

1. **`profiles` 표가 예전부터 있었다.** 저장소 SQL에는 정의가 없는데 DB에는
   있었다(Supabase 템플릿이나 손으로 만든 것). `create table if not exists`는
   조용히 건너뛰므로 뒤따르는 `sales_rep` 참조가 `42703`으로 죽었다.
   → 컬럼은 전부 `add column if not exists`로 따로 채운다.
2. **그 표의 기존 정책이 자기 자신을 조회해 `42P17`(무한 재귀)이 났다.**
   → 정책 안에서 `profiles`를 직접 읽지 않는다. 판정은 전부 SECURITY DEFINER
   함수(`my_role()`/`is_admin()`/`can_write()`)를 거친다 — RLS를 우회하므로
   재귀가 없다. 옛 정책은 이름을 모르더라도 `pg_policies`를 훑어 전부 지운다.

3. **표 목록을 손으로 적으면 빠진다.** 첫 실행 뒤에도 `product_accessories`가
   anon 전체 허용으로 남아 있었다(지금은 안 쓰는 표). → 마지막에 public 스키마의
   anon 정책을 전부 훑어 닫는 단계를 넣었다.

**RLS가 막혔는지 확인할 때 오류 유무로 판단하지 말 것.** RLS 거부는 오류가 아니라
**빈 결과**로 온다. `error`만 보면 "아직 열려 있다"고 잘못 읽는다. 행 수를 세야 한다.

`role`은 enum이 아니라 **text + check 제약**이다. 예전 표에 text로 있을 수 있고
형이 어긋나면 정책이 통째로 깨지기 때문이다.
실행 전에는 `profiles` 표가 없어 앱이 '준비되지 않았습니다'를 띄운다.

- 역할 네 가지: `pending`(**아무것도 못 봄**) / `viewer`(읽기만) /
  `sales`(읽기·쓰기) / `admin`(전부 + 삭제 + 계정관리)

### 스스로 가입하면 데이터가 통째로 열려 있었다 (2026-08-15 수정)

로그인 화면에 **Sign up 탭**이 있었고, 새 계정은 곧바로 `sales`가 됐다.
`can_read()`가 `my_role() is not null`이라 **역할이 있기만 하면 읽혔다.**
배포 주소는 공개돼 있으므로 **주소를 아는 누구나 가입 한 번으로 거래처
1,150곳·매출 15,221건을 읽고 쓸 수 있었다.** 구글 로그인도 같은 경로다.

이메일 확인도 꺼져 있다 — `heoniree@idibc.local`이 확인됨 상태로 만들어졌다.
`.local`은 메일이 안 가는 주소이므로, 확인 절차가 있었다면 계정이 생기지 않았다.

두 겹으로 막았다:
- **DB** — 새 계정은 `pending`으로 들어온다. `can_read()`가 읽을 수 있는 역할을
  적어 두므로 `pending`은 아무것도 못 본다. `execution/sql/pending_role.sql`
  (auth_and_roles.sql도 같은 값으로 맞춰 두었다).
- **화면** — 가입 탭·가입 코드를 없앴다. 계정은 관리자가 만든다.
  설정 > 계정 · 권한에서 승인 대기가 맨 위에 뜬다.

**화면에서 버튼만 지우면 안 된다.** 요청을 직접 보내면 그만이다. 그래서 DB가
본체이고 화면은 안내다. 바깥쪽 한 겹은 사람이 해야 한다 —
Supabase > Authentication > Sign In / Providers > Email >
**"Allow new users to sign up" 끄기**.
- **맨 처음 만들어지는 계정이 자동으로 admin**이다 — 단, `profiles`가 비어
  있을 때만이다. 이 프로젝트에는 표가 예전부터 있었고 행도 들어 있어서
  나중에 만든 `heoniree`가 `sales`로 들어갔다. 이럴 때는
  `execution/sql/promote_admin.sql`로 한 번 올린다 (화면에서는 못 올린다 —
  자기 역할을 스스로 바꾸는 것은 RLS가 막는다).
- 원칙적으로 첫 계정이 admin인 이유는 관리자를 손으로 심을 방법이
  없으면 아무도 못 들어온다. 두 번째부터는 sales로 들어오고 admin이 올려 준다.
- **읽기는 전사 공통이다.** KPI 수익성이 전사 기준이고 담당을 지정하지 않는 거래처가
  대부분이라(매출이 적거나 중요 거래처가 아니면 지정하지 않는 것이 운영 방식이다)
  담당별로 막으면 화면이 텅 빈다. 회사가 커지면 `can_read()`만 좁히면 된다.
- **삭제는 admin만.** 되돌릴 수단(감사 로그·휴지통)이 아직 없어 좁게 열어 뒀다.
- 역할 변경은 화면이 아니라 **DB에서 막는다** (`profiles` RLS). 화면에서 숨겨도
  요청을 직접 보내면 그만이다. 자기 역할은 스스로 못 바꾼다.

로그인은 **아이디 + 비밀번호**다. Supabase Auth는 이메일을 요구하므로
`toLoginEmail()`이 `VITE_LOGIN_DOMAIN`(기본 `idibc.local`)을 붙여 준다.
`heoniree` → `heoniree@idibc.local`. 계정·권한 관리는 설정 화면의 **계정 · 권한**.

### `onAuthStateChange` 안에서 DB를 부르면 안 된다

콜백은 Supabase의 auth 잠금 안에서 돈다. 그 안에서 `supabase.from(...)`을
`await` 하면 그 쿼리가 다시 세션을 기다리면서 서로 물려 **교착에 빠진다**.
콜백이 끝나지 않으니 `setLoading(false)`도 영영 안 돌고 화면이
'불러오는 중…'에서 멈춘다. **실제로 그렇게 멈춰서 앱이 안 열렸다.**

세션 콜백에서는 `setUser`/`setLoading`만 한다. 프로필은 `user?.id`를 보는
별도 effect에서 읽고, 그 결과가 화면을 막지 않게 한다.
네트워크가 응답하지 않을 때를 대비해 8초 안전장치도 뒀다.

### RLS 판정 함수는 반드시 `(select ...)`로 감싼다

`using (public.can_read())`라고 쓰면 Postgres가 이것을 **행 필터**로 보고
행마다 한 번씩 부른다. 매출이 15,221행이니 조회 한 번에 함수가 15,221번 돌고,
그때마다 `profiles`를 다시 읽는다. **RLS를 켠 뒤 첫 화면이 3~4배 느려졌던
원인이 이것이다.**

`using ((select public.can_read()))`로 감싸면 InitPlan이 되어 **조회당 한 번만**
평가된다. 정책을 새로 쓸 때 빠뜨리기 쉬우니 주의.

### 이미 들고 있는 것을 다시 조회하지 않는다

`useDashboardData`가 거래처 수·내 담당 거래처·이번 달 매출·후속조치를 **각각
다시 조회**하고 있었다. 넷 다 `DataContext`가 이미 들고 있는 것이라 순수한
낭비였고, 이번 달 매출은 `select *`로 그 달 전체를 받아 브라우저에서 걸렀다.

지금은 전부 `useData()`에서 파생한다 — **이 훅의 Supabase 조회는 0건이다.**
덤으로 휴지통에 든 행이 자동으로 빠진다(직접 조회는 그걸 거르지 않았다).

새 화면을 만들 때도 같은 원칙을 지킬 것: 거래처·매출·활동은 이미 로드돼 있다.

### `fetchData`는 동시에 두 번 돌지 않는다

매출 15,221행을 16쪽으로 나눠 받는 무거운 조회다. 화면·훅·모달이 제각각
`refreshData()`를 부르고 effect까지 겹치면 **같은 조회가 네 번 돈다** — 실제로
콘솔에 `Fetched 15221 records for sales`가 네 번 찍혔고, 그게 체감 3~4배의
정체였다. `inFlight` ref에 진행 중인 약속을 담아 두고, 이미 돌고 있으면 그것을
그대로 돌려준다. 부르는 쪽은 아무것도 몰라도 된다.

### 데이터는 로그인한 뒤에 한 번만 부른다

`DataContext`는 `DataProvider`가 `/login`까지 감싸고 있어서 로그인 화면에서도
돌 수 있다. 두 가지를 지킨다:

- **로그인 전에는 부르지 않는다.** RLS가 어차피 빈 결과를 주는데 매출 16쪽을
  로그인 화면에서 헛되이 받을 이유가 없다.
- **사용자당 한 번만 부른다** (`fetchedFor` ref). 단순히 '한 번만'으로 막으면
  로그인 화면에서 이미 써 버려서 로그인 뒤에 못 부른다.

### 서버 함수는 서비스 롤 키가 필요하다

`api/telegram-webhook.js`·`api/daily-digest.js`가 anon 키로 폴백하고 있었다.
RLS를 닫은 뒤로는 **`SUPABASE_SERVICE_ROLE_KEY`가 없으면 동작하지 않는다.**
Vercel 환경변수에 넣을 것 (VITE_ 접두어 금지). 없으면 시작할 때 콘솔에 경고가 뜬다.

실제로 이것 때문에 봇이 멈췄다. 증상이 헷갈리게 나왔다 — **RLS 거부는 조회에서
'빈 결과'로 오므로** `bot_allowlist`가 비어 보였고, 봇은 '처음 쓰는 사람'으로
판단해 자동 등록을 시도했다가 권한 오류로 죽으면서 *"schedules_and_inbox.sql을
실행했는지 확인하라"* 는 엉뚱한 안내를 냈다. 지금은 401/403/42501을 알아보고
서비스 롤 키를 넣으라고 정확히 안내한다.

### 개발 중 자동 로그인

`src/hooks/useDevAutoLogin.js`. `.env.local`에 아래를 적으면 개발 서버에서만
자동으로 로그인한다. **`import.meta.env.DEV` 안에 있어 배포 빌드에서는 통째로
잘려 나간다** (빌드 산출물에 문자열이 남지 않는 것을 확인했다).

```
VITE_DEV_AUTOLOGIN_ID=<아이디>
VITE_DEV_AUTOLOGIN_PW=<비밀번호>
```

**비밀번호를 저장소에 넣지 말 것.** 이 저장소는 공개돼 있다. `.env.local`은
`.gitignore` 대상이다. 배포된 앱에서도 Supabase가 세션을 담아 두고 토큰을
갱신하므로 로그인은 사실상 한 번만 하면 된다.

## '내가 누구인지' — 담당자 이름

로그인을 떼어냈던 동안 `user`가 늘 `null`이라 두 가지가 조용히 망가져 있었다
(지금은 로그인이 돌아왔고, 아래는 그때 고친 내용이다).

1. **`DataContext`가 데이터를 아예 안 불러왔다.** 세션이 없으면 `setLoading(false)`로
   끝내고 있었다. 거래처·매출·활동이 통째로 빈 배열이라 상단 카드가 '—',
   KPI가 0, 영업 코치가 0건이었다. 지금은 세션이 없어도 부른다 (RLS로 보호된다).
2. **`resolveSalesRep(null)`이 `null`을 돌려줬다.** '내 담당'이 하나도 안 잡힌다.
   지금은 대시보드 우측 상단의 **담당 고르는 칸**에서 정한 이름을 localStorage에
   담아 쓴다(`xavian_my_sales_rep`). 바꾸면 `my-rep-changed` 이벤트로 다시 계산한다.
   로그인이 되살아나면 계정 이름이 우선이고, 못 맞추면 저장값으로 떨어진다.

`KPIWidget.managedClientIds`도 깨져 있었다 — `myAccounts.map(c => c.id)`인데
`myAccounts`는 **id 문자열 배열**이라 전부 `undefined`였다. **로그인이 되면
오히려 KPI가 전부 0이 되는 상태**였고, 지금은 로그인이 없어 폴백이 돌아 우연히
맞고 있었다. 그 폴백에도 `'이헌일'`이 박혀 있어 다른 담당자가 봐도 이헌일 실적이
나왔다. 둘 다 고쳤다.

### 상단 '전체 거래처'는 거래처 수가 아니다

`dashboardStats.totalClientsCount`는 **2023년 이후 매출이 있는 client_id의 개수**다
(679곳). `clients` 표의 행 수(1,150)와 다르다. 이름이 헷갈리므로 주의.

## 담당자 지정은 '중요 거래처'에만 한다

거래처 1,150곳 중 담당이 지정된 곳은 소수다. **이건 누락이 아니라 운영 방식이다** —
매출이 적거나 중요 거래처가 아니면 담당을 지정하지 않는다.

2026-08 기준으로 담당이 채워진 것은 **관리자(이헌일) 한 명뿐**이다. 박민철·송원기는
아직 개발 단계라 세팅 전이고, 그들 이름으로 잡히는 4곳·1곳은 실제 담당 범위가 아니다.

그래서 **'담당 미지정 매출이 절반이 넘는다'는 수치를 결함으로 읽으면 안 된다.**
두 사람의 담당이 들어오면 자연히 채워진다. (한 번 그렇게 읽고 일괄 지정 기능을
최우선으로 잡았다가 되돌렸다.)

## 담당 거래처는 활동으로 정해진다

미팅·통화한 곳은 당연히 담당이다. 그런데 신규·복원 영업 대상은 거래처에
`sales_rep`이 비어 있는 경우가 많아, **정작 공들이는 곳이 화면에서 사라진다.**
영업 코치에서 빠지고 KPI 정기적방문횟수에도 안 잡힌다(둘 다 담당 거래처만 센다).
실측으로 11곳이 이렇게 새고 있었다 — 케이피한석유화(누적 3.5억)까지 포함해서.

- `DataContext.addActivity`가 활동을 넣을 때 **담당이 비어 있으면 채운다.**
  이미 담당이 있으면 건드리지 않는다(남의 거래처를 뺏으면 안 된다).
- `activities.user_name`에 누가 다녀왔는지 저장한다. 담당 판정의 근거다.
- 과거분 일괄 보정: `node execution/assign_rep_from_activities.mjs --rep 이헌일 --apply`
  (기본은 미리보기)

덕분에 영업 코치는 `clients.sales_rep`만 보면 된다. 예전처럼 '활동이 있으면 내 것'
같은 우회 판정을 두면 `user_name`이 빈 옛 활동 때문에 남의 거래처까지 딸려온다
(바커케미칼·롯데정밀화학이 그렇게 떴다).

## 대체공휴일은 계산한다

`koreanHolidays.js`의 `withSubstituteHolidays()`. 손으로 적으면 빠진다 —
실제로 2026-08-17(광복절이 토요일이라 생기는 대체공휴일)이 누락돼 있었다.

관공서의 공휴일에 관한 규정 제3조를 그대로 옮겼다:
- 삼일절·광복절·개천절·한글날·부처님오신날·성탄절·어린이날이 토/일과 겹치면 대체
- 설날·추석은 **다른 공휴일(일요일 포함)과 겹칠 때만** — 토요일은 대상이 아니다
- 현충일은 대체 대상이 아니다

이미 표에 적힌 대체공휴일이 있으면 또 만들지 않는다. 이 검사를 빼면 3/1(일)에
3/2가 이미 있는데도 3/3을 하나 더 만들어 낸다.
**음력 공휴일(설·추석·부처님오신날)의 날짜 자체는 여전히 손으로 넣어야 한다.**

## 영업 코치 수동 보정 (`src/utils/coachOverrides.js`)

규칙만으로는 못 맞추는 것이 있다. CRM 데이터가 사실을 다 담고 있지 않아서다.

- **한독산업(주)** — CRM에는 2026년 매출부터 있어 '첫 거래 성사'로 잡히지만,
  실제로는 예전에 거래하다 끊겼다가 되살아난 곳이다.
- **주식회사 윌슨플로켐** — 휴브글로벌 자회사로 원래 거래하던 곳이다. 신규가 아니다.

두 저장소를 합쳐 쓴다:
- `KNOWN_FACTS` — 코드에 박아둔 사실. 어느 기기에서 봐도 같게 적용된다.
- localStorage — 화면의 '제외' 버튼. 기기마다 따로 남는다. **화면 값이 우선한다.**

`kind`: `hide`(코치에서 뺌) / `existing`(기존 거래처) / `restored`(복원된 곳).

### 신규 영업 정렬은 진행도 순

접점 횟수가 곧 진행도다(여러 번 만났다 = 얘기가 되고 있다).
`score = 접촉횟수×100 + max(0, 90-경과일) + min(60, 공들인주수)`.
실측 상위: OCI 6회(6개월째) > 위코퍼레이션 4회 > 대달인터내셔널 4회.

## 이름이 달라 자동 감지가 안 되는 중복 거래처

`execution/merge_clients_manual.mjs`의 `MERGES`에 적어 합친다.
`merge_duplicate_clients.mjs`는 이름 정규화로 찾으므로 **글자가 실제로 다른**
중복은 못 잡는다. 2026-08 병합 실적:

| 없앤 것 | 남긴 것 |
|---|---|
| 윌스플로켐 | 주식회사 윌슨플로켐 |
| 진영IBC | 진영IBC (최은성) |
| 신성물산(주) | 대달인터내셔널(주) (대달산업으로도 불림) |

매출은 한쪽에, 활동은 다른 쪽에 갈려 있으면 코치가 같은 회사를 '매출 0인 신규'와
'거래 중인 기존'으로 따로 세어 판단이 어긋난다.
합친 뒤 `clientAliases.js`와 `import_daily_report.mjs`의 `ALIASES` **양쪽에** 별칭을 넣어야
다음 업로드에서 다시 만들어지지 않는다.

## 거래처 브리핑 (`ClientBriefing.jsx` + `api/client-briefing.js`)

코치에서 거래처를 누르면 열린다. 만나기 직전에 보는 한 장이다.

활동 메모에는 쓸 만한 정보가 다 들어 있다 — 경쟁사, 월 사용량, 상대 단가,
기술 스펙, 걸림돌, 하기로 한 것. 다만 여러 달에 흩어져 있어 매번 스무 건을
다시 읽을 수 없다. 그걸 대신 읽어 접어 준다.

### 세 층으로 나눈다 — 숫자와 글을 섞지 않는다

1. **숫자** — 앱이 계산한다 (매출 추이·마지막 거래·접촉 횟수·주요 품목).
2. **정리** — 모델이 **글로 적힌 것만** 읽어 만든다
   (단계 / 핵심사실 / 걸림돌 / 따낼 물량 / 경쟁 / 다음 할 일 / 놓칠 위험).
3. **원문** — 날짜순 활동 기록. 정리가 미덥지 않으면 여기서 확인한다.

**모델에 숫자를 맡기지 않는 게 핵심이다.** 계산된 수치는 프롬프트에 넣어 주되
"다시 계산하거나 바꾸지 마라"고 못박는다. 모델이 매출을 지어내면 영업 판단이 틀어진다.

### 캐시

활동 건수와 마지막 활동일을 지문(`fingerprint`)으로 삼아 localStorage에 담는다.
새 활동이 없으면 다시 부르지 않는다 — 한 번 부르는 데 13초쯤 걸리고 비용도 든다.
재방문은 1.2초. '다시 정리' 버튼으로 강제 갱신할 수 있다.

캐시된 단계는 코치 목록에 배지로 뜬다. **목록에서 새로 부르지 않는다** — 거래처가
수십 곳이라 열 때마다 호출하면 화면이 멈춘다.

### 단계 (`STAGES`)

`중단 · 보류 · 초기 접촉 · 정보 파악 · 견적 제출 · 샘플 진행 · 단가 협의 · 발주 임박 · 거래 중`

실측(OCI주식회사, 활동 6건): '보류 / 놓칠 위험 높음 / 경쟁사 테스트로 진행 보류,
8월 재논의 예정' — 담당자명·일정·걸림돌까지 전부 기록에 있는 사실만 나왔다.

## 거래처 목록 기본 정렬 (`/clients`)

챙겨야 할 순서대로 세운다. 예전에는 가나다순이라 1,148곳 중 지금 볼 곳을
찾으려면 매번 검색을 해야 했다.

| 군 | 조건 | 정렬 |
|---|---|---|
| 1군 | 내 담당 **and** 매출 있음 | 매출 많은 순 |
| 2군 | 활동이 있는 곳 (영업 중) | 활동 많은 순 → 최근 접촉순 |
| 3군 | 나머지 | 매출 많은 순 |

실측(2026-08): 1군 42곳 / 2군 39곳 / 3군 1,067곳.
2군 첫머리는 OCI 6회, 아토텍코리아 5회 — 지금 붙고 있는 신규 건들이다.

거래처는 회사명으로 묶어 보여주므로(`groupedClients`) 매출·활동도 **회사 단위로
합산**해서 순위를 매긴다. 같은 회사가 여러 행으로 나뉘어 있어도 한 덩어리로 선다.

### 거래처 거르기 · 저장된 보기

`src/utils/clientFilters.js` + `tests/clientFilters.test.mjs`(14건).
1,150곳에 검색창 하나뿐이라 매번 같은 조건을 손으로 찾고 있었다.

조건 6개: 내 담당 / 매출 있음 / 최근 3개월 거래 / 6개월 무거래 / 영업 중 / 연락처 없음.
여러 개를 고르면 **모두** 만족해야 한다(AND). 하나도 안 고르면 전부 통과.

**'6개월 무거래'에 거래 이력이 없는 곳을 넣으면 안 된다.** 그러면 목록이 신규
후보로 가득 차서 정작 챙길 휴면 거래처가 묻힌다. 테스트가 이 규칙을 고정한다.

휴면 판정은 마지막 **거래**일 기준이다 — 마지막 **접촉**일(활동)과 다르다.
정렬용 `last`와 헷갈리지 말 것.

자주 쓰는 조합은 이름을 붙여 저장한다(localStorage `xavian_client_views`).
같은 이름으로 저장하면 덮어쓴다. 표를 하나 더 만들 만큼 무거운 값이 아니다.

### 계정 → 영업사원 이름

`src/utils/salesRep.js`의 `resolveSalesRep`. `clients.sales_rep`은 한글 이름인데
로그인 계정은 이메일·영문이라 그대로는 못 맞춘다. 예전에는 이 대응이
`useDashboardData` 안에만 있어서 다른 화면에서 '내 담당'을 판별할 수 없었다.
영업사원이 늘면 `SALES_REP_OPTIONS`와 `NAME_MAPPING`을 함께 고친다.

## 후속조치 알림 루프

`src/utils/followUps.js` + `api/daily-digest.js` + 달력 옆 '하기로 한 것'.

`activities.next_action_date`(다음 조치일) 칸은 원래 있었는데 **어디서도 보여주지
않아 아무도 쓰지 않았다** (225건 중 7건). 분석이 아무리 좋아도 다음 행동을 잊지 않게
하는 장치가 없으면 놓친다. 실제로 **191일 지난 건**(미원화학 견적서 제출)이
어디에도 안 보인 채 묻혀 있었다.

### 완료 판정에 컬럼을 두지 않는다

**기한일 이후에 그 거래처와 접촉한 기록이 있으면 처리된 것으로 본다.**
체크박스를 하나 더 만들면 그것도 안 누르게 된다. 이미 남기는 활동 기록으로 판단한다.
`tests/followUps.test.mjs` 10건이 이 규칙을 고정한다 (특히 '기한 전에 만난 것은
처리로 보지 않는다').

### 아침 브리핑에 파이프라인이 들어간다

보내는 것: 오늘 일정 → 오늘 하기로 한 것 → 기한 지난 것 →
**이번 주 마감 예정 기회** → **멈춘 기회** → 오래 못 간 곳.

아침에 알아야 할 건 두 가지다 — **이번 주에 떨어질 것**과 **멈춰 있는 것**.
정체 기준은 화면과 같은 값을 쓴다(샘플 60일, 나머지 21~30일). 두 곳의 숫자가
다르면 어느 쪽을 믿어야 할지 알 수 없다.

`deals` 표가 없어도(마이그레이션 전) 브리핑은 그대로 나간다 — 조회가 실패하면
빈 배열로 넘어간다. 단계 이름이 한글이라 URL 필터에 넣지 않고 받아서 거른다.

### 아침 브리핑

Vercel Cron이 매일 `0 22 * * *`(UTC) = **한국시간 07:00**에 `/api/daily-digest`를 부른다.
보내는 것: 오늘 일정 → 오늘 하기로 한 것 → 기한 지난 것 → 오래 못 간 곳.
받는 사람은 `bot_allowlist`에 등록된 대화다.

손으로 확인하려면 `?key=<deriveSecret(봇토큰)>`을 붙인다. Cron 호출은
`x-vercel-cron` 헤더로 알아본다.

### 입력 쪽도 채운다

텔레그램으로 "다음주에 견적 보내기로 함" 같은 말이 섞이면 봇이 `nextDate`·`nextDetail`을
뽑아 활동과 함께 저장한다. 이게 다음 날 아침 브리핑의 재료가 된다.

## 휴대폰 연락처 가져오기

`src/utils/contactImport.js` + `tests/contactImport.test.mjs`(16건).
설정 > **휴대폰 연락처 가져오기**.

거래처 1,150곳 중 연락처가 있는 곳이 71곳뿐인데 정작 휴대폰에는 다 들어 있다.
활동 메모에서 뽑은 이름·직급은 있어도 **전화번호는 한 건도 없다** — 메모에
안 적기 때문이다. 번호는 휴대폰에서만 온다.

내보내는 법: `contacts.google.com` → 내보내기 → Google CSV 또는 vCard.
(휴대폰 연락처 앱에서 .vcf로 저장해도 된다.)

**한글이 깨지는 지점이 세 군데 있다. 전부 실제로 나온다:**

1. 안드로이드 기본 내보내기는 대개 **vCard 2.1 + QUOTED-PRINTABLE**이다.
   `N;ENCODING=QUOTED-PRINTABLE:=EC=9D=B4=ED=97=8C=EC=9D=BC` 를 그대로 읽으면
   이름이 `=EC=9D=B4…`로 나온다. UTF-8 바이트로 모아 한 번에 디코딩해야 한다.
2. 줄이 75자를 넘으면 **접혀서** 다음 줄이 공백으로 시작한다. 이어 붙이지 않으면
   값이 잘린다.
3. **QP 소프트 개행은 접힘과 다르다.** 줄 끝이 `=`이고 다음 줄이 **공백 없이**
   이어진다. 2번 규칙만 있으면 이어지는 줄에 콜론이 없어 통째로 버려지고,
   값이 UTF-8 한 글자 중간에서 잘린다 (실제로 `범우화학공업 강<?>=` 로 나왔다).
   `=`로 끝나는 줄을 무조건 잇지는 말 것 — BASE64 사진의 패딩(`==`)도 `=`로
   끝난다. **그 줄이 QUOTED-PRINTABLE이라고 밝힌 경우에만** 잇는다.

**회사명이 정확히 맞는 것만 자동으로 붙인다.** 그 밖은 **후보로만** 올리고 사람이
고른다. 자동 반영은 위험하다 — `와이파인텍 김창수`가 `파인텍`으로 잡히는데 실제로
다른 회사일 수 있다. 이미 같은 이름이 있는 거래처는 건너뛴다.

개인폰에는 거래처 담당자가 거의 없어(1,099명 중 4명 일치) 후보 제안이 없으면
쓸모가 없었다. 이름에 회사를 적는 습관을 잡는다(`남양화학 장부장`, `인지산업 사장`).

단서 없는 연락처는 접어 두고 필요할 때 펼친다 — 업무폰도 1,719명이 여기 남는다.

구글 주소록 최신 내보내기(36열)에서 열 이름이 바뀌었다. 부분 일치로 찾으면
`Phone`이 **`Phonetic First Name`**에, `E-mail`이 **`E-mail 1 - Label`**에 걸린다.
`findColumn`이 정확 일치를 먼저 보고, 부분 일치에서는 `label`로 끝나는 열을 건너뛴다.

전화번호 정규화는 **글자가 섞여 있으면 원본을 지킨다.** `내선 1234`에서 숫자만
남기면 `1234`가 되어 무슨 번호인지 알 수 없다 (테스트가 고정한다).

### 업무용 폰은 이름 칸에 회사·사람·직급을 한 줄로 적는다

`범우화학공업 강병국 팀장`, `금호피앤비 여수 구자성 전무(공장장)`.
거래처 밑에 그대로 넣으면 `범우화학공업 > 범우화학공업 강병국`이 되어 읽기 나쁘다.

`refineContact(연락처, 거래처명)`이 나눈다. **그 거래처 이름과 겹치는 앞부분만**
걷어낸다 — 아무 말이나 지우지 않는다. `금호피앤비 여수 구자성`에서 `여수`는
금호피앤비화학의 일부가 아니므로 **남긴다**(공장 구분이라 지우면 정보가 사라진다).
`(주)`·`유한회사`처럼 `companyKey`가 통째로 걷어내는 토막은 건너뛰고 이어서 본다.

직급은 **vCard의 `TITLE`이 우선**이고, 없을 때만 이름 끝말에서 짐작한다.
끝말이 직급 목록에 없으면 떼지 않는다(`김진만 주임 이천공장`은 그대로 둔다).

### 개인폰과 업무폰은 완전히 다른 파일이다

같은 사람의 두 휴대폰인데 결과가 딴판이다. **업무폰을 받아야 한다.**

| | 개인폰 (구글 CSV) | 업무폰 (.vcf) |
|---|---|---|
| 연락처 | 1,099명 | 2,306명 |
| 회사명 있음 | 103명 | 812명 |
| 직급 있음 | 89명 | 530명 |
| 회사명 일치 | **4명** | **328명** |
| 후보 | 10명 | 259명 |
| 붙는 거래처 | 4곳 | **298곳** |

### 일괄 반영은 `execution/import_phone_contacts.mjs`

```bash
node execution/import_phone_contacts.mjs "<연락처.vcf>"           # 미리보기
node execution/import_phone_contacts.mjs "<연락처.vcf>" --apply   # 반영
```

화면과 **같은 판독기**(`src/utils/contactImport.js`)를 쓴다. 결과가 어긋나면
어느 쪽을 믿어야 할지 알 수 없다.

후보는 **거래처 이름으로 시작하는 것만** 넣는다. 그냥 '들어 있으면'으로 잡으면
`영주이앤씨 이정운`이 `주식회사 이앤씨`로 붙는다 — 다른 회사다.
**회사명 칸과 이름 칸을 따로 본다.** 이어 붙여 보면 회사명이 달리 적힌 것이
(`한경 TEC` vs 거래처 `한경티이씨주식회사`) 앞을 막아 이름 쪽이 안 보인다
(이 때문에 맞는 15명이 걸러지고 있었다).

**실제 반영(2026-08-15):** 497명 / 거래처 267곳.
연락처 있는 거래처 71곳 → **307곳**, 담당자 95명 → **592명**(전화번호 524명).
건너뛴 후보 10명은 화면에서 하나씩 고르면 된다
(`KPX 그린케미칼`↔`그린케미칼`, `세스코 씨비티`↔`씨비티` 등).

**대표 담당자(`is_primary`)가 없으면 화면에 전화번호가 안 뜬다.** 거래처마다
첫 사람을 대표로 세운다. 예전에 손으로 넣은 6곳이 대표 없이 남아 있어 함께 채웠다.

### 같은 사람인지는 **전화번호**로 가린다

처음 연락할 때는 상대가 누구인지 모른 채 번호만 저장한다(`발주담당`).
관계가 진행되고 명함을 주고받으면 이름·직급·이메일을 적어 **다시 저장**한다.
그래서 한 사람이 여러 건으로 남는다. **이름으로 묶으면 못 잡는다** —
이름이 달라서 두 건이 된 것이기 때문이다.

`phoneKey`(숫자만) 가 기준이고, 한 건으로 접을 때 `contactScore`가 높은 쪽을
남긴다. 사람 이름다운 것을 가장 높게 친다 — 그게 이 표의 목적이다.

| 진다 | 이유 |
|---|---|
| `발주담당` · `생산` · `구매팀` | 사람이 아니라 자리 |
| `신우첨단소재` · `가든프로젝트` | 회사명을 사람 칸에 적었다 |
| `노재성 공장장/` | 직급이 이름 칸으로 흘러들었다 |
| `수성테크&화학 양수만` · `FTC Korea 박우람` | 회사명이 앞에 붙었다 |

**진 쪽을 통째로 버리지 않는다.** 빈 칸을 진 쪽에서 채운다 — 나중에 적어 둔
이메일·직급은 명함에서 옮긴 것이라 가장 아깝다. 이메일이 여럿이면 **도메인이
성한 쪽**을 고른다(`evakim@mwc.o.kr` 오타 vs `evakim@mwc.co.kr`).
한 글자짜리 도메인 조각은 거의 오타다.

```bash
node execution/merge_duplicate_contacts.mjs           # 미리보기
node execution/merge_duplicate_contacts.mjs --apply   # 반영
```

**거래처가 다르면 합치지 않는다.** 회사를 옮겼거나 거래처가 중복 등록된 것일 수
있다. 목록으로만 보여준다 (실측 3건: `성신드럼`↔`현대그린텍`,
`애경화학`↔`애경케미칼`, `KCC`↔`케이씨씨 전주3공장`).

**진 쪽의 `is_primary`를 먼저 내려야 한다.** 거래처당 대표는 하나라는 유니크
제약(`idx_single_primary_contact`)이 있어서, 이긴 쪽을 먼저 대표로 세우면
아직 대표인 진 쪽과 부딪혀 통째로 실패한다. **지운 표시만으로는 제약이 풀리지
않는다** — `deleted_at`을 채워도 인덱스는 그 행을 본다.

가져오기 경로(화면·스크립트)도 같은 규칙을 쓴다. 올린 파일 안에서 번호가 같으면
접고, 이미 그 번호가 있는 거래처면 **이름이 달라도 넣지 않는다.**

실측(2026-08-15): 19묶음 39행 → 19명. 최종 **575명 / 307곳**
(전화번호 507 · 직급 474 · 이메일 133), 대표 없는 곳 0.

`client_contacts`의 직급 컬럼 이름은 `role`이 아니라 **`department_role`**이다.

## 거래처 담당자 채우기

`execution/extract_contacts.mjs`. 거래처 1,148곳 중 연락처가 있는 곳이 22곳뿐이라,
만나러 가면서 '누구를 찾아야 하지'를 매번 활동 메모에서 뒤져야 했다.

활동 메모의 `[담당자] 유재민 책임` 태그(일일업무보고서 반영 때 붙인 것)에서
이름·직급을 뽑아 `client_contacts`에 넣는다. 가장 자주 만난 사람이 `is_primary`다.

- **태그 안만 본다.** 본문 전체를 정규식으로 훑으면 '앞으로도 대표', '때문에 대표'
  처럼 문장 조각이 사람 이름으로 잡힌다.
- 이미 연락처가 있는 거래처는 건드리지 않는다 (손으로 넣은 것이 더 정확하다).
- **전화번호는 뽑을 수 없다.** 활동 메모 225건에 전화번호가 하나도 없다.
  이름·직급만 채우고 번호는 명함 촬영이나 직접 입력으로 채워야 한다.

반영 결과(2026-08): 49곳 / 담당자 67명 추가 → 연락처 있는 곳 22곳 → 71곳.
내 담당 81곳 기준으로는 69곳이 채워졌다.

`DataContext`가 대표 담당자를 `contact_person` / `contact_role` / `phone`으로
펴서 내려준다. 거래처 브리핑에서 전화번호를 누르면 `tel:` 로 바로 걸린다.

## 견적서 · 발주서 (`/quotes`, `/purchase-orders`)

스키마는 `execution/sql/quotes_and_orders.sql` (한 번 실행).

### PDF는 브라우저 인쇄로 만든다

**jsPDF·html2canvas를 쓰지 않는다.** 한글 폰트를 따로 심어야 하고, 표가 이미지로
나가 글자가 뭉갠다. 브라우저 인쇄(Ctrl+P → 'PDF로 저장')가 한글도 표도 깨끗하고
의존성도 늘지 않는다. 스타일은 `index.css`의 `.doc-sheet` / `@media print`.
`@page { size: A4 }`로 크기를 고정하고, 인쇄 시 `.doc-sheet` 밖은 전부 감춘다.

### 견적서에는 사진이 들어간다

고객이 보는 문서라 "그래서 뭘 주는 건데"가 한눈에 보여야 한다.
품목 사진 + 악세서리(상부캡·밸브) 사진이 함께 나간다.
**밸브에는 '무밸브'가 하나의 선택지다** — '없음'이 아니라 이름이 있는 형태이고,
그 부위 사진을 따로 올린다.

발주서는 우리가 협력업체에 보내는 문서라 사진 없이 일반 양식으로 낸다.

### 문서번호는 저장할 때 DB를 보고 짓는다

`src/utils/docNumber.js`. `Q-20260814-01` / `PO-20260814-01` 형식.

**화면에 든 목록을 세면 안 된다.** 예전에는 `list.filter(오늘).length + 1` 이었는데
세 가지가 깨졌다:

1. 오늘 것 하나를 지우면 번호가 되돌아가 이미 있는 번호와 부딪힌다
   (`quote_no`는 unique라 저장이 실패하고 사용자는 이유를 모른다)
2. 두 사람이 같은 시각에 새로 만들면 둘 다 같은 번호를 받는다
3. 창을 열어 두고 한참 뒤 저장하면 그 사이에 생긴 번호와 부딪힌다

그래서 **개수가 아니라 최대값 + 1**을 쓰고, **저장 직전에** DB에서 그 날짜 번호만
`like`로 좁혀 읽는다. 그래도 겹치면 다시 지어 재시도한다(최대 5회).
마지막 방어는 DB의 unique 제약이다.

`tests/docNumber.test.mjs` 15건이 고정한다. 실제 DB로도 확인했다 —
가운데를 지워도 되돌아가지 않고, 동시에 3건을 저장해도 겹치지 않았다.

### 파이프라인 — 영업 기회는 별도 레코드다

`execution/sql/deals.sql` (한 번 실행) + `src/utils/dealStages.js` +
`tests/dealStages.test.mjs`(13건).

예전에는 **거래처를 `clients.status`로 묶어** 보여줬다. 네 가지가 안 됐다:

- 거래처 하나가 단계 하나만 가진다. 한 곳과 두 건을 동시에 진행하면(하나는
  협상, 하나는 이제 접촉) 표현할 방법이 없다.
- 금액이 없다. 파이프라인에 얼마가 걸려 있는지 알 수 없다.
- 예상 마감이 없다. 이번 달에 뭐가 떨어질지 못 본다.
- 성사되면 status가 바뀌면서 **그 건의 기록이 사라진다.** 수주율을 못 낸다.

Pipedrive·HubSpot·Salesforce가 전부 '기회'를 별도 레코드로 두는 이유다.
거래처는 관계이고, 기회는 건별로 뜬다.

단계: `리드 · 접촉 · 제안 · 샘플 · 협상` + 닫힘 `수주 · 실패`.
**`샘플`이 한 단계인 것이 이 회사의 특징이다** — 화학·식품 용기라 고객이 반드시
샘플을 받아 테스트하고 거기서 몇 달이 걸린다.

지켜야 할 것:

- **확률은 단계에 붙는다.** 사람이 매번 감으로 적으면 값이 제각각이라 합계가
  의미를 잃는다. 건별로 덮어쓸 수는 있다.
- **정체는 `stage_changed_at`으로 잰다.** `updated_at`은 메모만 고쳐도 바뀌어서,
  손댈 때마다 '방금 움직인 건'이 되어 영영 안 잡힌다. DB 트리거가 채운다.
- **정체 기준은 단계마다 다르다.** 샘플 60일, 나머지 21~30일. 똑같이 두면
  샘플 건이 전부 경고로 떠서 경고가 의미를 잃는다.
- **수주율은 닫힌 건만 놓고 센다.** 진행 중인 건을 분모에 넣으면 기회를 많이
  만들수록 수주율이 떨어지는 이상한 지표가 된다. 닫힌 건이 없으면 `null`(—)이다.
- 단계 이름은 DB의 check 제약과 코드가 **같아야 한다.** 한쪽만 고치면 저장이
  조용히 실패한다.

### 견적서를 내면 기회가 자동으로 잡힌다

`src/services/dealSync.js`. **이중 입력이 파이프라인이 죽는 가장 큰 이유다.**
견적서를 만들고 나서 파이프라인에 같은 내용을 또 넣으라고 하면 아무도 안 넣는다.
그러면 보드가 비고, 비어 있으니 안 보게 되고, 안 보니 더 안 넣는다.

견적 하나에 기회 하나(`deals.quote_id`)로 묶는다. 견적 상태가 바뀌면 단계도
따라간다 — 발송/작성중 → 제안, 수주 → 수주, 실패/취소 → 실패.

**더 진행된 단계를 뒤로 끌어내리지 않는다.** 견적을 낸 뒤 손으로 '협상'으로
옮겨 뒀는데 견적서를 한 번 고쳤다고 '제안'으로 돌아가면 옮긴 일이 헛수고가
된다. 앞으로만 민다.

**기회를 못 만들어도 견적서 저장은 성공이다.** 부수적인 일이 본업을 막으면
안 된다 (`deals` 표가 없으면 조용히 건너뛴다).

영업 코치의 각 줄에도 **파이프라인에 올리는 단추**(가지 모양)가 있다. 코치가
'지금 챙길 곳'을 찾아 줘도 이름·금액을 손으로 다시 적어야 하면 그 한 단계에서
실제로 안 하게 된다. 코치에서 올린 건은 `접촉` 단계로 들어간다 — 이미 얘기가
오간 곳이기 때문이다.

## 거래명세서 (`/statements`)

**새 표를 만들지 않는다.** 이미 쌓여 있는 매출을 거래처·기간으로 잘라 문서로
낸다. 따로 저장하면 매출과 명세서가 어긋나는 순간 어느 쪽이 맞는지 알 수 없다.

고객은 이걸 받아 자기 장부와 맞춰 보고 결제한다. 그래서 견적서와 달리
**사진을 넣지 않고** 날짜·품목·수량·단가를 한 줄씩 보여준다.

`receivables`에 그 달 스냅샷이 있으면 **잔액·연체를 함께 붙인다** — 결제를
재촉하는 실제 근거가 그것이다. 없으면 명세만으로도 문서는 성립한다.

기본 기간은 **지난달**이다. 보통 마감 후에 보낸다.

### PDF는 파일 이름까지 정해서 저장한다

`src/utils/printDoc.js`. 그냥 `window.print()`를 부르면 저장되는 파일 이름이
브라우저 탭 제목(`아이앤디 CRM | 견적서`)이 된다. 견적서를 여러 장 받아 두면
어느 것이 어느 거래처인지 알 수 없다.

**브라우저는 저장할 때 `document.title`을 파일 이름으로 쓴다.** 그래서 인쇄
직전에 제목을 `견적서_Q-20260815-01_(주)한솔케미칼`로 바꿔 두고 `afterprint`에
되돌린다. 그 이벤트를 안 주는 브라우저를 위해 1.5초 보험도 뒀다.

### 회사 정보와 안내문구는 화면에서 고친다

설정 > **회사 정보 · 문서 기본값**(`CompanyProfilePanel`). 예전에는 SQL로 한 번
넣어 두고 고칠 방법이 없어서 전화번호 하나 바꾸려 해도 개발자를 거쳐야 했다.

안내문구(`company_profile.quote_terms` / `po_terms`,
`execution/sql/document_defaults.sql`)는 **모든 문서에 같은 말이 붙는 자리**다.
건별로 다른 말은 견적서의 `notes`(비고)에 들어간다. 둘을 섞지 말 것.

**여기서 고친 값은 다음에 만드는 문서부터 반영된다.** 이미 발행한 견적서는
발행 시점 값을 통째로 복사해 두므로 그대로 남는다 — 나중에 회사 정보가
바뀌었다고 예전 견적서가 달라지면 안 된다.

### 발행 시점 값을 통째로 복사한다

`quote_items`에 품목명·규격·사진 URL·단가와 `accessories`(jsonb)를 그대로 담는다.
카탈로그가 나중에 바뀌어도 **그때 낸 견적서는 그때 모습 그대로여야 하기 때문이다.**

### 캡·밸브도 품목이다 (`products.type`)

악세서리를 따로 표에 두면 **사진을 두 번 올려야 하고 이름이 갈린다.**
그래서 `product_accessories` 표는 쓰지 않고 `products.type` 하나로 나눈다:

| type | 뜻 | 개수(2026-08) |
|---|---|---|
| `IBC` / `드럼` / `제리캔` | 완제품 — 견적 품목으로 고른다 | 109 / 2 / 7 |
| `밸브` | 상부캡·밸브 고르기에 뜬다 | 6 |
| `캡` | 〃 | 22 |
| `부품` | 그 밖 (지그·명판·보호대 등) | 17 |

분류 규칙 (사용자가 알려준 대로):
- `BF-`/`MF-`/`RF-` 로 시작 = IBC 모델, `DRUM-` = 드럼, `제리캔 NNL` = 제리캔
- 완제품이 아니면서 이름이 **'밸브'로 끝나면** 밸브 (`50V1밸브`).
  `밸브연결 커플러`·`탱크밸브보호대`는 밸브 종류가 아니라 부품이므로 끝자리로 가른다.
- 이름에 `캡` 또는 `CAP`이 있으면 캡 (`R-CAP`도 잡으려면 영문까지 봐야 한다)
- 나머지는 부품

**'무밸브'는 품목으로 하나 만들어 두었다.** IBC 모델명(`BF-무밸브`)에만 있고 독립
품목이 없었는데, 밸브 선택지로 골라야 하고 그 부위 사진도 따로 필요하기 때문이다.

### 사진 등록

설정 > **품목 · 악세서리 사진**. Supabase Storage 공개 버킷 `product-images`에
올린다. 올릴 때 최대 900px·JPEG 0.85로 줄인다 (견적서에는 26mm로 들어가므로
원본을 그대로 두면 문서만 무거워진다). 투명 PNG는 흰 배경을 깔아 인쇄에서
검게 나오는 것을 막는다.

### 회사 e카탈로그에서 사진 채우기

`execution/apply_catalog_images.mjs`. 사진 163장을 손으로 찍는 대신
회사 e카탈로그 PDF(`IND_eCatalog_2026.pdf`)에서 뽑아 붙였다 — 163개 중 **138개**.

```bash
node execution/apply_catalog_images.mjs <사진폴더>            # 미리보기
node execution/apply_catalog_images.mjs <사진폴더> --apply    # 반영
```

- **같은 사진을 여러 품목이 나눠 쓴다.** IBC 109개가 BF/MF/RF/탱크/검정 사진
  6장이면 끝난다. 파일은 한 번만 올리고 주소를 돌려쓴다.
- **이미 사진이 있는 품목은 건드리지 않는다.** 손으로 올린 것이 더 정확하다
  (실제 납품 사양일 수 있다). 덮어쓰려면 `--force`.
- 사진 파일은 저장소에 넣지 않는다. PDF에서 다시 뽑으면 된다.

PDF에서 뽑는 쪽은 일회성이라 스크립트로 남기지 않았다. PyMuPDF + Pillow로
`doc.extract_image(xref)` → smask(투명도)를 흰 배경에 합성 → 900px JPEG.
**페이지를 잘라내면 안 된다** — 캡션 글씨('150 CAP' 등)가 사진 안에 같이 들어간다.
원본 이미지를 xref로 뽑아야 깨끗하다.

#### 모델명 → 사진 규칙

모델명이 곧 사양이다: `BF-A,50V1H(검)I중/소`

| 자리 | 뜻 |
|---|---|
| `BF`/`MF`/`RF` | 투입구 캡 150 / 225 / 430 |
| `50`/`80` | 밸브 구경 2" / 3" |
| `B`/`V` | 버터플라이 / 볼 |
| `1`/`2` | 노즐형 / 커플러형 |
| `(E)` | 대전방지(ESD) |
| `검` | Black Color |
| `(상판)` | 상판절개형 |
| `탱크` | 프레임 없는 탱크 단품 |

**판정 순서가 곧 우선순위다.** 탱크는 프레임이 아예 없어 생김새가 가장 다르므로
맨 앞에 둔다. `바다사랑부표`는 IBC로 분류돼 있지만 부표라 카탈로그에 없다 — 제외.

밸브 6종의 `B`/`V` 해석은 **추론이다.** 3인치가 버터플라이에만 있어
(카탈로그 p4·p8) B=버터플라이, V=볼로 읽었다. 틀리면 화면에서 사진만 바꾸면 된다.

#### 아직 사진이 없는 25개

밸브캡 4종·오링류·명판·지그·밴드 등 카탈로그에 실리지 않는 소모품이다.
직접 찍어 올려야 한다.

### 우리 회사 정보

`company_profile` 한 줄. idibc.kr에서 수집해 SQL에 넣어 두었다:
아이앤디 주식회사 / 대표 이대현 / 142-81-76012 /
경기도 용인시 처인구 백암면 삼백로 367-20 / 031-334-9625 / idibc@daum.net
계좌·직인 이미지는 비어 있으므로 필요하면 직접 채운다.
